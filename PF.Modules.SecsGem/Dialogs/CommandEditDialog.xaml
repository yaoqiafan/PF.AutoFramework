<UserControl
    x:Class="PF.Modules.SecsGem.Dialogs.CommandEditDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:PF.Modules.SecsGem.Dialogs"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    MinWidth="360"
    mc:Ignorable="d">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
    </UserControl.Resources>
    <Grid Margin="15">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!--  Stream / Function 行  -->
        <StackPanel
            Grid.Row="0"
            Margin="0,0,0,12"
            Orientation="Horizontal">
            <TextBlock
                Width="70"
                VerticalAlignment="Center"
                Text="Stream (S):" />
            <TextBox
                Width="80"
                Padding="4,2"
                IsEnabled="{Binding IsStreamEnabled}"
                Text="{Binding Stream, UpdateSourceTrigger=PropertyChanged}" />
            <TextBlock
                Width="80"
                Margin="15,0,0,0"
                VerticalAlignment="Center"
                Text="Function (F):" />
            <TextBox
                Width="80"
                Padding="4,2"
                IsEnabled="{Binding IsFunctionEnabled}"
                Text="{Binding Function, UpdateSourceTrigger=PropertyChanged}" />
        </StackPanel>

        <!--  IsReadOnly 提示（仅当 S/F 被锁定时显示）  -->
        <TextBlock
            Grid.Row="1"
            Margin="0,0,0,8"
            FontSize="11"
            Foreground="#757575"
            Text="Stream 和 Function 已由所选分组预填，可根据需要修改。"
            Visibility="{Binding IsStreamReadOnly, Converter={StaticResource BoolToVis}}" />

        <!--  命令名称  -->
        <StackPanel Grid.Row="2" Margin="0,0,0,12">
            <TextBlock
                Margin="0,0,0,4"
                Text="命令名称 (如 Are You There Request):" />
            <TextBox
                Padding="4,2"
                Text="{Binding CommandName, UpdateSourceTrigger=PropertyChanged}" />
        </StackPanel>

        <!--  操作按钮  -->
        <StackPanel
            Grid.Row="4"
            HorizontalAlignment="Right"
            Orientation="Horizontal">
            <Button
                Width="80"
                Height="30"
                Margin="0,0,10,0"
                Command="{Binding ConfirmCommand}"
                Content="确定"
                IsDefault="True" />
            <Button
                Width="80"
                Height="30"
                Command="{Binding CancelCommand}"
                Content="取消"
                IsCancel="True" />
        </StackPanel>
    </Grid>
</UserControl>
