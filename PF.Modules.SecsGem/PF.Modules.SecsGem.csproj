<Project Sdk="Microsoft.NET.Sdk">
	<Import Project="..\Common.Desktop.props" Condition="Exists('..\Common.Desktop.props')" />
	<ItemGroup>
		<PackageReference Include="Microsoft.EntityFrameworkCore" />
		<PackageReference Include="Microsoft.EntityFrameworkCore.Sqlite" />
		<PackageReference Include="Prism.Wpf" />
	</ItemGroup>
	<ItemGroup>
	  <ProjectReference Include="..\PF.CommonTools\PF.CommonTools.csproj" />
	  <ProjectReference Include="..\PF.Infrastructure\PF.Infrastructure.csproj" />
	  <ProjectReference Include="..\PF.UI.Infrastructure\PF.UI.Infrastructure.csproj" />
	</ItemGroup>

	<PropertyGroup>
		<IsModuleProject>true</IsModuleProject>
		<IsPackable>true</IsPackable>
		<Version>1.0.5</Version>
		<PackageReleaseNotes>
v1.0.5 (2026-08-03) — 服务管理适配配置路径项目隔离

[变更]
- SecsServiceManagerViewModel 的服务 exe 路径解析下沉到 PF.CommonTools 的
  ServicePathResolver（同一份实现供本面板与 PFApplicationBase 启动校验共用），
  原私有 ResolveServiceExePath 删除。解析优先读 SCM 中实际注册的 ImagePath，
  修复了此前注册表键名与 Setup.iss 写入值不一致（带/不带空格）导致查找恒失败、
  静默回退到 Program Files 默认路径的问题。
- "安装服务"按钮在注册服务前，先把当前主程序的项目名写入服务目录的 appsettings.json。
  这条安装路径绕过安装器，服务缺少 ProjectName 键会直接拒绝启动。

[说明]
服务名仍为 SecsGemService，不按项目区分：其本机通道端口 6800 在 Worker.LocationServer
与 InternalClient 两端硬编码，HSMS 被动端口也唯一，全机只能有一个实例真正工作。

v1.0.4 (2026-07-09) — SECS/GEM 调试侧边栏图标调整

[变更]
- SecsGemDebugView Icon：DebugIcon → Antenna，避免与其它调试页共用同一图标，并适配
  PF.Application.Base v1.0.3 新增的侧边栏悬停描边动画。

v1.0.3 (2026-07-07) — 适配 PF.SecsGem.DataBase 工作单元(UoW)模式重构（破坏性变更）

[破坏性变更 — 数据访问模式]
- SecsParameterViewModel/SecsCommandBuilderViewModel/SecsConnectionViewModel 迁移至新的
  BeginScope() 工作单元模式（对应 PF.SecsGem.DataBase v1.0.3 的 ISecsGemDataBase API 重写）。
  旧的 ISecsGemDataBase.GetRepository()/SaveChangesAsync() 已删除；若有下游代码直接调用
  这些旧接口，需要同步改为：
    using var scope = db.BeginScope();
    var repo = scope.GetRepository&lt;T&gt;();
    ...
    await scope.SaveChangesAsync();
- 依赖 PF.SecsGem.DataBase &gt;= 1.0.3。

v1.0.2 (2026-06-17) — SecsParameterViewModel 代码整理 + 命名空间引用补全

[变更]
- SecsCommandBuilderViewModel：补充 using PF.Infrastructure.SecsGem.Command.Interaction，
  修复因缺少该引用在某些编译环境下的 CS0246 潜在错误。
- SecsParameterViewModel：全量代码格式化（去除列对齐空格，统一紧凑风格），
  无逻辑变化，仅提升代码可读性与 diff 一致性。
- SecsLogViewModel：小幅格式调整（空行），无逻辑变化。
- 依赖 PF.Infrastructure 1.0.2（SecsGem 初始化逻辑重构）。

v1.0.1 - 框架初始独立版本
		</PackageReleaseNotes>
		<Description>SECS/GEM 通信模块：报文监控与发送 UI</Description>
		<GeneratePackageOnBuild Condition="'$(Configuration)' == 'Release'">true</GeneratePackageOnBuild>
	</PropertyGroup>
</Project>