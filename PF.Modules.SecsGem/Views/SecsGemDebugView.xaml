<UserControl x:Class="PF.Modules.SecsGem.Views.SecsGemDebugView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PF.Modules.SecsGem.Views"
             mc:Ignorable="d" 
             d:DesignHeight="1080" d:DesignWidth="1920">
    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="5"/>
            <RowDefinition Height="250"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0"  CornerRadius="3" Padding="10">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <Ellipse Width="16" Height="16" Fill="{Binding StatusColor}" Margin="0,0,10,0"/>
                <TextBlock Text="{Binding ConnectionStatusText}" VerticalAlignment="Center" Margin="0,0,20,0" FontWeight="Bold"/>

                <Button Content="初始化" Command="{Binding InitializeCommand}" Width="110" Margin="0,0,10,0"/>

                <Button Content="连接" Command="{Binding ConnectCommand}" Width="100" Margin="0,0,10,0"/>
                <Button Content="断开" Command="{Binding DisconnectCommand}" Width="100"/>
            </StackPanel>
        </Border>

        <Grid Grid.Row="1" Margin="0,5,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="280"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="280"/>
            </Grid.ColumnDefinitions>

            <GroupBox Grid.Column="0" Header="命令库 (Command Library)">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <WrapPanel Grid.Row="0" Margin="0,0,0,5">
                        <Button Content="导入" Command="{Binding ImportCommandsCommand}" Margin="0,0,5,5" Width="80"/>
                        <Button Content="导出" Command="{Binding ExportCommandsCommand}" Margin="0,0,5,5" Width="80"/>
                        <Button Content="重新加载" Command="{Binding ReloadCommandsCommand}" Margin="0,0,0,5" Width="80"/>
                    </WrapPanel>

                    <TabControl Grid.Row="1">
                        <TabItem Header="主动命令">
                            <TreeView ItemsSource="{Binding IncentiveCommandsTree}"/>
                        </TabItem>
                        <TabItem Header="应答命令">
                            <TreeView ItemsSource="{Binding ResponseCommandsTree}"/>
                        </TabItem>
                    </TabControl>
                </Grid>
            </GroupBox>

            <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Stretch"/>

            <GroupBox Grid.Column="2" Header="报文编辑器 (Message Builder)">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="{Binding CurrentCommandHeader, FallbackValue='当前选中: S1F1 (Are You There Request)'}" Margin="5" FontWeight="Bold"/>

                    <TreeView Grid.Row="1" ItemsSource="{Binding CurrentMessageNodes}" Margin="0,5" BorderBrush="#CCC" BorderThickness="1">
                        <TreeView.ItemTemplate>
                            <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                                <StackPanel Orientation="Horizontal" Margin="2">
                                    <TextBlock Text="[" Foreground="Blue"/>
                                    <TextBlock Text="{Binding ItemFormat}" Foreground="DarkRed" FontWeight="Bold"/>
                                    <TextBlock Text=", " Foreground="Blue"/>
                                    <TextBlock Text="{Binding Length}" Foreground="DarkGreen"/>
                                    <TextBlock Text="] " Foreground="Blue"/>

                                    <TextBox Text="{Binding Value, UpdateSourceTrigger=PropertyChanged}" 
                                             Width="150" Margin="5,0" 
                                             />

                                    <CheckBox Content="IsVariable (VID)" IsChecked="{Binding IsVariableNode}" Margin="5,0,0,0" VerticalAlignment="Center"/>
                                </StackPanel>
                            </HierarchicalDataTemplate>
                        </TreeView.ItemTemplate>
                    </TreeView>

                    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,5,0,0">
                        <CheckBox Content="等待回复" IsChecked="{Binding WaitReply}" VerticalAlignment="Center" Margin="0,0,15,0"/>
                        <Button Content="更新变量节点" Command="{Binding UpdateVariablesCommand}" Margin="0,0,10,0" Padding="10,5"/>
                        <Button Content="发送 (Send)" Command="{Binding SendCommand}" Background="#007ACC" Foreground="White" Padding="20,5" FontWeight="Bold"/>
                    </StackPanel>
                </Grid>
            </GroupBox>

            <GridSplitter Grid.Column="3" Width="5" HorizontalAlignment="Stretch"/>

            <GroupBox Grid.Column="4" Header="参数配置 (Parameters)">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <ComboBox Grid.Row="0" SelectedIndex="0" Margin="0,0,0,10">
                        <ComboBoxItem Content="System Params"/>
                        <ComboBoxItem Content="Validate Params"/>
                        <ComboBoxItem Content="Formula Params"/>
                    </ComboBox>

                    <DataGrid Grid.Row="1" AutoGenerateColumns="True" HeadersVisibility="Column" MinHeight="200"/>

                    <WrapPanel Grid.Row="2" Margin="0,10,0,0" HorizontalAlignment="Right">
                        <Button Content="导入" Command="{Binding ImportParamsCommand}" Margin="0,0,5,5" Width="60"/>
                        <Button Content="导出" Command="{Binding ExportParamsCommand}" Margin="0,0,5,5" Width="60"/>
                        <Button Content="保存参数" Command="{Binding SaveParamCommand}" Margin="0,0,0,5" Width="80" Background="#E8F5E9"/>
                    </WrapPanel>
                </Grid>
            </GroupBox>
        </Grid>

        <GridSplitter Grid.Row="2" Height="5" HorizontalAlignment="Stretch"/>

        <GroupBox Grid.Row="3" Header="实时通信日志 (Transaction Logs)">
            <ListBox ItemsSource="{Binding TransactionLogs}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" FontFamily="Consolas" FontSize="13"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </GroupBox>
    </Grid>
</UserControl>
