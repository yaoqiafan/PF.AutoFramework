<UserControl x:Class="PF.UI.Infrastructure.Dialog.InputDialogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PF.UI.Infrastructure.Dialog"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="{Binding Message}" FontSize="14" Margin="0,0,0,10" TextWrapping="Wrap"/>

        <TextBox Grid.Row="1" Text="{Binding InputText, UpdateSourceTrigger=PropertyChanged}" 
                 VerticalAlignment="Top" Height="32" Padding="5" FontSize="14" />

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="确定" Command="{Binding OkCommand}" Style="{DynamicResource ButtonPrimary}" Width="80" Height="32" Margin="10,0,0,0"/>
            <Button Content="取消" Command="{Binding CancelCommand}" Style="{DynamicResource ButtonDefault}" Width="80" Height="32" Margin="10,0,0,0"/>
        </StackPanel>
    </Grid>
</UserControl>
