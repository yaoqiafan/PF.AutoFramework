<Project Sdk="Microsoft.NET.Sdk">
	<Import Project="..\Common.Desktop.props" Condition="Exists('..\Common.Desktop.props')" />
  <ItemGroup>
    <PackageReference Include="Prism.Wpf" />
  </ItemGroup>
  <ItemGroup>
    <ProjectReference Include="..\PF.UI.Infrastructure\PF.UI.Infrastructure.csproj" />
    <ProjectReference Include="..\PF.Infrastructure\PF.Infrastructure.csproj" />
  </ItemGroup>

	<PropertyGroup>
		<IsModuleProject>true</IsModuleProject>
		<IsPackable>true</IsPackable>
		<Version>1.0.5</Version>
		<PackageReleaseNotes>
v1.0.5 (2026-08-28) — 界面操作日志接入：键目录 + 全量控件标记

[新增]
- OperationLogKeysAlarm 键目录（[OperationLogKeyCatalog]，内层嵌套类名 = 页面名，const 字段 = Key，
  DescriptionAttribute = 默认描述），模块 OnInitialized 里 OperationLogKeyRegistry.
  RegisterAssembly(Assembly.GetExecutingAssembly()) 一行注册。
- 本模块页面根挂 OperationLog.PageName、可交互控件挂 OperationLog.Key：2 个页面
  14 个键，其中 5 个关键操作（清除全部报警、清除选中报警、清除单条报警、系统复位、确认报警）默认启用记录；其余默认关闭，
  按项目在配置目录的 OperationLogConfig.xlsx 里勾选 Enabled 列即可打开，改过的值永远优先。
- 未标记/不支持的控件（密码框、Slider、DatePicker、页内导航选择器等）不产生任何日志。


			v1.0.4 (2026-07-13) — 报警弹窗界面图片显示异常

			[变更]
			Image Source="{Binding Alarm.ImagePath}" Stretch="Uniform"
			


			v1.0.3 (2026-07-09) — 报警中心侧边栏图标调整

			[变更]
			- AlarmCenterView 侧边栏图标由 ErrorGeometry 改为 Pulse：原图标为碎片化线稿，配合
			PF.Application.Base v1.0.3 新增的侧边栏悬停描边动画时观感差，Pulse 描边路径连贯完整。

			v1.0.2 (2026-07-07) — 报警详情大图解码限制 + KeepAlive 适配

			[修复]
			- AlarmDetailCardView.xaml 报警详情图（部分报警码可配置的产品图，最大 8500x5000px）
			无解码尺寸限制，改用显式 BitmapImage + DecodePixelHeight=900，单次解码内存开销从
			约 150MB 降到几 MB。
			- AlarmCenterViewModel 补充 KeepAlive =&gt; true，配合 PF.UI.Infrastructure v1.0.2 的
  RegionMemberLifetime 默认值变更，保留"允许重复导航至本视图"的既有语义。

v1.0.1 - 框架初始独立版本
		</PackageReleaseNotes>
		<Description>异常处理展示模块：报警触发、活跃报警看板、历史查询、SOP排故面板</Description>
		<GeneratePackageOnBuild Condition="'$(Configuration)' == 'Release'">true</GeneratePackageOnBuild>
	</PropertyGroup>
</Project>
