<Project Sdk="Microsoft.NET.Sdk">
	<Import Project="..\Common.props" Condition="Exists('..\Common.props')" />

	<PropertyGroup>
		<IsPackable>true</IsPackable>
		<Version>1.0.7</Version>
		<PackageReleaseNotes>
v1.0.7 (2026-07-17) — Modbus 主站契约补强：自动重连 + 可取消连接 + 原始报文透传（含破坏性变更）

[破坏性变更]
- IModbusRtuMaster/IModbusTcpMaster 新增 AutoReconnect（意外断开后是否自动重连）与
  ReconnectIntervalMs（重连间隔）两个属性；IModbusTcpMaster.ConnectAsync 签名新增
  可选 CancellationToken 参数（此前无 token 的连接尝试在目标不可达时按 OS 默认超时
  阻塞约 21s，外部编排无法打断）；IModbusMaster 新增 SendRawAsync（原始 PDU 透传）
  与 BuildFrame（构建完整请求帧供预览）两个方法；IModbusTcpMaster 新增
  TransactionIdAutoIncrement（MBAP 事务号是否逐请求递增）、FixedTransactionId（固定模式下
  使用的事务号）属性与 ResetTransactionId() 方法。下游若有这些接口的自定义实现类需补齐成员后才能编译；框架内置实现
  （PF.Infrastructure 1.0.8 的 ModbusRtuMaster/ModbusTcpMaster）已同步，仅经接口
  调用的代码源码级兼容、重编译即可。

[新增]
- IModbusMaster.SendRawAsync：发送原始 PDU（功能码+数据，1~253 字节）并等待一帧响应，
  返回原始响应 PDU。与 8 个内置读写方法不同——不校验功能码回显，也不把从站异常响应
  解包成 ModbusException，异常响应原样返回（首字节最高位置 1）。用于框架未覆盖的
  功能码或自定义诊断报文。
- IModbusMaster.BuildFrame：构建与实际发送帧逐字节一致的完整请求帧（RTU 含从站地址
  +CRC16，TCP 含 MBAP 头，TransactionId 以 0x0000 占位），仅供报文预览/记录，不产生 IO。
- IModbusTcpMaster.TransactionIdAutoIncrement/FixedTransactionId/ResetTransactionId：MBAP
  事务号是否逐请求递增（默认 true）、固定模式下使用的事务号、重置计数器；固定事务号模式
  （FixedTransactionId）用于兼容个别按固定事务号应答的网关或与抓包比对（会削弱靠事务号
  识别迟到/串包响应的能力，仅建议调试场景）。

v1.0.6 (2026-07-16) — 新增 Modbus 主站通讯契约

[新增]
- ICommunication 通讯管理框架新增 Modbus 支持：IModbusMaster（共享操作契约，8 个功能码
  01/02/03/04/05/06/0F/10）+ IModbusRtuMaster/IModbusTcpMaster（分别对齐 ISerialCommunication/
  IClient 的既定拆分方式，只做 Master/客户端角色）。
- CommunicationCategory 新增 Modbus 成员。
- 新增 ModbusExceptionCode 枚举（标准异常码 0x01~0x0B）与 ModbusException（携带 UnitId/
  FunctionCode/ExceptionCode），从站返回异常响应时抛出。

v1.0.5 (2026-07-15) — 锁存接口 LtcMode 默认值对齐（行为变更）

[行为变更]
- IAxis.SetLatchMode 与 IMotionCard.SetSoftWareLatchMode/SetLtcLatchMode 的可选参数
  LtcMode 默认值由 0 改为 1，与实现层（BaseAxisDevice/LTDMCMotionCard 均默认 1）及
  现有生产调用对齐——此前经接口调用与经类调用会取到不同默认值（C# 可选参数按调用点
  静态类型在编译期固化）。下游若有经接口调用且省略 LtcMode 的代码，重编译后默认值
  从 0 变为 1；显式传参的调用不受影响。

v1.0.4 (2026-07-09) — SecsGemNodeMessage 大端字节序统一 + DataType 格式码修正（含破坏性变更）

[破坏性变更]
- DataType.I1（原值 6，非标准格式码）改为 0b01100100（0x64）；DataType.I2（原值
  0b011000100，9 位数值已溢出字节范围）改为 0b01101000（0x68）。均修正为符合 SECS-II
  标准的格式码——此前两个值本身就是错误的，用其构造/解析的 I1/I2 报文与遵循标准协议的
  对端不兼容；修正后线格式才正确，但会改变此前"错误但已在跑"的现场行为，故按破坏性处理。

[修复]
- SecsGemNodeMessage 构造函数重写（SecsGemNodeMessage.cs）：
  - F4/F8/I8(long) 分支此前遗漏大端转换，浮点与 long 值以主机序（小端）直接写入 Data，
    对端解析出乱值；现与 I2/I4/U2/U4/U8 一致统一转大端序。
  - I4 分支此前仅匹配 `is Int64`，传入最自然的 `int` 完全不命中；命中时又把 8 字节数据
    错误标记为 4 字节 I4 类型，对端按声明长度解析会错位。改用 Convert.ToInt32 统一接受
    任意可转换的数值输入。
  - 构造函数末尾的防呆校验此前是死代码（SubNode 有属性初始化器恒不为 null，判断条件
    永假），类型不匹配时会静默产出 Data=null 的空节点，序列化后对端收到长度为 0 的字段
    且无任何报错；现改为在数据未成功构造时正确抛出 ArgumentException。
  - 明确约定并落地：SecsGemNodeMessage.Data 一律存放大端序（SECS-II 线格式）字节，
    序列化器原样输出、解析器不再修改 Data（配合 PF.Infrastructure 同版本的解析器/
    VID 更新器修复，见该包 changelog）。

v1.0.3 (2026-07-07) — 通讯契约 + HALCON 视觉数据模型 + 新增报警码

[新增]
- ICommunication/ICommunicationManagerService 契约、CommunicationCategory/Role 枚举、
  CommunicationUIAttribute；FileTransfer 传输通道全套接口/实体/事件；ISerialCommunication。
- HALCON 视觉：EngineMode/RoiOp/RoiType/VisionRoiConfig/ProcedureSignature 等枚举与接口。
- AlarmCodesExtensions 新增 Detection.ResumeException（编号 50202）。

v1.0.2 (2026-06-17) — 报警系统 SECS/GEM 支持 + SecsGemNodeMessage 修复

[破坏性变更]
- AlarmInfoAttribute 构造函数新增三个必填参数（签名已变更，现有标注须更新）：
    messageEn     — 英文报警描述，上传 SECS/GEM S5F1 消息用，限 35 字符
    messageID     — 整型报警 ID，框架内置从 50000 起步，需开发者保证唯一性
    messageIDHex  — 英文报警文本（限 40 字符），与 messageEn 保持一致
  旧签名：(category, message, severity, solution, imagePath?)
  新签名：(category, message, messageEn, severity, solution, messageID, messageIDHex, imagePath?)

[新功能]
- AlarmInfoAttribute 新增属性：MessageEn、MessageID、MessageIDHex。
- AlarmInfo 模型新增属性：MessageEn、MessageID、MessageIDHex，
  报警字典查询结果完整携带 SECS/GEM 上传元数据。
- AlarmRecord 模型新增属性：MessageEn、MessageID、MessageIDHex，
  触发记录完整携带上传所需字段，无需在上传侧二次查字典。
- AlarmCodes 内置 26 条框架报警全量更新 [AlarmInfo] 特性：
    HW 类（硬件）：ServoError=50000 … MonitorFailure=50025
    SYS 类（系统）：InitializationTimeout=50016 … UndefinedStep=50021
    SAFE 类（安全）：SafeDoorOpen=50022 … MonitorFailure=50025
  所有英文描述均控制在 35 字符内，符合 SECS/GEM ALID 上报规范。

[Bug 修复]
- SecsGemNodeMessage.SetValue(DataType.U4)：
  将 `if (_value is uint uU4)` 强类型守卫改为 Convert.ToUInt32(_value)，
  修复 int / long / short 等整型赋值时因类型不匹配而静默跳过、Data 字段保持旧值的问题。
  TypedValue 由 uU4 改为存储原始 _value，与其他 DataType 分支行为一致。

v1.0.1 - 框架初始独立版本
		</PackageReleaseNotes>
		<Description>PF.AutoFramework 核心契约层：接口、枚举、特性，无任何外部依赖</Description>
		<GeneratePackageOnBuild Condition="'$(Configuration)' == 'Release'">true</GeneratePackageOnBuild>
	</PropertyGroup>
</Project>
