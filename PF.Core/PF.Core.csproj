<Project Sdk="Microsoft.NET.Sdk">
	<Import Project="..\Common.props" Condition="Exists('..\Common.props')" />

	<PropertyGroup>
		<IsPackable>true</IsPackable>
		<Version>1.0.28</Version>
		<PackageReleaseNotes>
v1.0.28 (2026-09-17) — IGenICamNodeAccess 新增 FeatureFilePath 只读属性 + ReimportFeatureFileAsync 手动重新导入

[新增 — IGenICamNodeAccess，相机与采集卡通用]
- FeatureFilePath：固定路径属性文件的完整路径（只读），供调试面板展示，不用再猜目录/文件名。
- ReimportFeatureFileAsync(CancellationToken token = default)：手动从 FeatureFilePath 重新
  导入一次，语义与连接时的自动导入一致（文件不存在则建占位并返回 false）。现场把新导出的
  文件放到固定路径后，不需要重新连接设备就能重新下发。

子包版本对照：PF.Infrastructure 1.0.26→1.0.27，PF.Modules.Debug 1.0.17→1.0.18。

v1.0.27 (2026-09-17) — IGenICamNodeAccess 新增 FeatureFileMissing 只读属性，接线属性文件改由设备自己在连接时导入

[新增 — IGenICamNodeAccess，相机与采集卡通用]
- FeatureFileMissing：固定路径属性文件在最近一次连接时是否缺失。此前这套"固定路径读文件、
  缺失置位"的逻辑是项目侧检测模组自己写的，现在下沉成相机/采集卡设备类自己的能力——
  GenICam 节点操作历来归设备自己管（本接口既有的 ImportFeatureFileAsync 就是这个约定），
  不该由业务机构代劳，否则每个用到线扫相机/采集卡的项目都要各写一份。

子包版本对照：PF.Infrastructure 1.0.25→1.0.26，PF.Modules.Debug 1.0.16→1.0.17。

v1.0.26 (2026-09-17) — LineScanCameraConfig.ScanMode 默认值订正为 null，兑现类注释早就写的承诺（含破坏性变更，同一类事故的第三处）

[破坏性变更 — LineScanCameraConfig]
- ScanMode 默认值从 "LineScan" 改为 null。本类自己的类注释一直写着"所有可空字段的语义统一为：
  null = 不下发、沿用设备当前值"，但 ScanMode 实际默认值是具体字符串 "LineScan"，是本类唯一
  没兑现这条承诺的字段——不填就等于替调用方选了 LineScan，这对某些型号/接线（编码器接在采集卡
  上、经外部信号线转发的场景需要 FrameScan）是错的，相当于拿相机已经调好的真实配置去覆盖。
  和 v1.0.24/1.0.25 的 LineTriggerConfig.Mode / FrameControlConfig.TriggerOnCameraSide
  是同一次事故复盘挖出来的同一类问题，这是第三处，也是最后一处默认值不是"不下发"的字段。

子包版本对照：其余子包本次无变化（消费方 PF.WorkStation.MappingShift 已同步简化）。

v1.0.25 (2026-09-17) — FrameControlConfig.TriggerOnCameraSide 改为可空，未表态即探测兜底（含破坏性变更，同一类事故的第二处）

[破坏性变更 — FrameControlConfig]
- TriggerOnCameraSide 从 bool（默认 false）改为 bool?（默认 null）。false 曾经在"调用方没
  下发这个字段"和"调用方明确要卡控帧"两种情况下取值相同，设备层没法区分，只能一律当卡控帧
  处理；改成可空后 null 才是"调用方没表态"，由设备层退回连接时探测到的相机实际状态兜底，
  true/false 仍然是调用方的明确选择，原样生效。
- 和 v1.0.24 的 LineTriggerConfig.Mode 是同一次事故复盘挖出来的同一类问题：一次只想测曝光、
  完全没碰 FrameControl 的调用，被当成"要卡控帧"，路由到没有 StreamTriggerEnable 节点的卡上，
  报 MV_E_GC_ACCESS——跟行触发那次是同一个根因（"没设置"被当成"设置成默认值"），只是发生在
  帧触发这一侧。

子包版本对照：PF.Infrastructure 1.0.24→1.0.25。

v1.0.24 (2026-09-17) — LineTriggerConfig.Mode 改为可空，未指定即不下发（含破坏性变更，修复真实误覆盖事故）

[破坏性变更 — LineTriggerConfig]
- Mode 从 LineTriggerMode（默认 InternalRate）改为 LineTriggerMode?（默认 null）。null 语义
  统一为"不下发、沿用相机当前接线"，与本类其余字段（TriggerSource/Encoder 为 null 即不下发）
  的既有约定对齐；调用方显式赋值一个具体枚举值时行为不变。
- 起因是一次真实误覆盖：LineScanDetectionModule.BuildConfig 之前会在调用方完全没设置行触发
  （Mode 仍是默认 InternalRate 且未给 TriggerSource/Encoder）时自动兜底成 Encoder 模式——
  一次只想测曝光、没碰行触发的调试调用，把现场已经验证并通过 FeatureLoadEx 导入好的
  ExternalLine（编码器接在采集卡上、经 LinkTrigger0 转发）接线，静默改回了错的 Encoder 直连。
  Mode 原来是非空枚举，"调用方没设置"和"调用方明确要 InternalRate"在类型上无法区分，
  框架只能靠猜；改成可空后 null 就是"没设置"的唯一、明确的表示，不再需要猜。

子包版本对照：PF.Infrastructure 1.0.23→1.0.24。

v1.0.23 (2026-09-17) — IGenICamNodeAccess 新增设备属性文件导入能力

[新增 — IGenICamNodeAccess，相机与采集卡通用]
- ImportFeatureFileAsync(string filePath, CancellationToken token = default)：从设备属性
  文件（如 MVS 客户端导出的 .mfs）批量导入并下发节点值，比在代码里逐个节点手抄字面量更可靠——
  现场调好参数、导出成文件后直接喂给设备即可。个别节点导入失败不中断整体导入，只记日志。
- 背景：本次是为了验证"能不能直接读取现场导出的相机/采集卡参数文件"这条路是否可行，
  先在 PF.Modules.Debug 1.0.16 的线扫相机调试页加了测试入口，尚未接入任何生产路径。

子包版本对照：PF.Infrastructure 1.0.21→1.0.22，PF.Modules.Debug 1.0.15→1.0.16。

v1.0.22 (2026-09-16) — 线扫检测运动参数收口到轴点位表，成像参数交给相机自身配置（含破坏性变更）

[破坏性变更 — ScanProfile 删除，新增 ScanGeometry]
- ScanProfile 曾自己持有一整套运动参数（起点/终点/扫描速度/定位速度/加速度/减速度/S曲线时间/
  加减速余量）与成像参数（行间距/曝光/最大行频/帧触发开关/帧超时倍数/轴超时），和机台上其它轴
  统一用的"点位表 + 点位名"模式重复，且 SCurveTimeMs 字段名标"ms"、实际按秒透传给轴（与
  AxisPoint.STime 的秒单位对不上），是个从未被发现的 1000 倍单位错误。
- 新增 ScanGeometry(AxisPoint start, AxisPoint end) 取代：不再持有可独立编辑的运动参数，
  StartMm/EndMm/VelocityMmPerSec/AccelerationMmPerSec2/DecelerationMmPerSec2/SCurveTimeSec
  全部由传入的起点/终点两个 AxisPoint 直接换算（速度/加减速/S曲线时间取自终点点位），也不再
  计算 FrameHeightLines/ActualLineRate/MaxExposureTimeUs——帧长/行频这类成像参数已经配置在
  相机自身（UserSetDefault），不再由软件重新计算下发覆盖。Validate() 只保留"起点终点位置
  是否相同""终点速度是否大于0"两条几何校验，行频/曝光相关校验一并移除。
- 调用方（PF.Infrastructure 1.0.21 的 LineScanDetectionModule.ScanAsync）同步改造为按起点/
  终点点位名调用，不再传 ScanProfile。

[新增 — FrameControlConfig]
- TriggerOnCameraSide：挂了采集卡时，帧触发（含软触发命令）是否仍下发到相机自身而不是采集卡
  节点树，默认 false 不影响现有行为。"挂了采集卡"与"由采集卡控帧"是两件独立的事，海康官方
  LineScanSoftwareTrigger 样例即使枚举到 CameraLink/XoF 设备也全程只用 device.Parameters，
  从不触碰 IInterface，此前框架按"挂卡即卡控帧"二选一的假设并不总成立。

子包版本对照：PF.Infrastructure 1.0.20→1.0.21，PF.Modules.Debug 1.0.14→1.0.15。

v1.0.21 (2026-09-15) — 辅助编码器新增输入模式设置；修复 1.0.20 尚未发布就存在的硬件锁存空引用隐患（含破坏性变更）

[新增 — IMotionCard]
- SetEncoderMode(int Channel, EmcoderModeEnum InMode, int Mulit = 1, CancellationToken token = default)：
  设置辅助编码器的输入模式（脉冲方向 / AB相），对应板卡 SDK 的 dmc_set_extra_encoder_mode。

[新增]
- EmcoderModeEnum（PF.Core.Enums.Hardware）：辅助编码器输入模式枚举，脉冲方向 / AB相 两个取值。

[破坏性变更 — IAuxEncoder]
- 移除 SetLatchModeAsync / GetLatchNumberAsync / GetLatchPositionAsync 三个方法，新增
  SetMode(EmcoderModeEnum InMode = EmcoderModeEnum.AB相, int Mulit = 1, CancellationToken token = default)。
  辅助编码器的职责收窄为"设置自己的输入模式"：1.0.20 已经把硬件锁存收口到 IAxis 一个入口，
  IAuxEncoder 上这三个锁存方法自那以后就没有任何调用方，属于死接口，直接删除；项目里若自写了
  IAuxEncoder 实现，必须删掉这三个方法、补上 SetMode 才能编译。

[破坏性变更 — IAxis，同时是对 1.0.20 设计缺口的修复]
- SetLatchMode / GetLatchNumber / GetLatchPos 新增可选参数 IAuxEncoder encoder = null：显式传入
  时以调用方指定的编码器为准（用于需要临时越过绑定关系操作另一路编码器的场景）；缺省（null，
  即 1.0.20 发布时唯一支持的调用方式）时自动取本轴已挂载的 IAuxEncoder，未挂载抛
  InvalidOperationException——这正是 1.0.20 changelog 承诺的行为，但当时的 BaseAxisDevice
  实现漏了这层兜底：硬件锁存分支直接对 encoder 取 .Channel，encoder 缺省是 null，必定
  NullReferenceException，相当于把"未挂载编码器"从一条可读的 InvalidOperationException
  退化成了裸崩溃。【1.0.20 从未推送到私有源，全仓库当时也没有任何调用点走到这条分支，
  这颗雷没有在已发布版本里存在过、也没有真的炸过】。调用方源码级兼容：不传 encoder 的
  既有调用点行为不变（沿用 1.0.20 文档描述的自动解析语义）。

子包版本对照：PF.Infrastructure 1.0.19→1.0.20（同一批级联修复，见该包 changelog）；
其余子包本次无变化。

v1.0.20 (2026-09-14) — 轴位置锁存收回辅助编码器通道参数，改为轴内部自动调用绑定的辅助编码器（含破坏性变更）

[破坏性变更 — IAxis]
- SetLatchMode / GetLatchNumber / GetLatchPos 去掉 1.0.16～1.0.17 加的 int Encoder（GetLatchPos
  还去掉了 double Mulit）三个参数：硬件锁存（LatchType=1）该锁哪一路辅助编码器，改为由本轴当前
  挂载的 IAuxEncoder（硬件配置里把该编码器的 ParentDeviceId 指向本轴）自动决定，调用方不再需要
  ——也不再能够——手动指定通道号/倍率。此前把 IAuxEncoder 抽成独立设备契约后，轴的硬件锁存分支
  却仍要调用方替它传通道号，两条口径不一致，容易和挂载配置的实际编码器对不上；现在收回，位置
  锁存仍只有 IAxis 一个入口，IAuxEncoder 这个接口不出现在调用方视野里。
- 调用方源码级不兼容：按位置传参、且传了 Encoder/Mulit 的调用点编译期报错；仅按名字传到
  token 的调用点不受影响（本仓库内既有调用点均如此）。硬件锁存（LatchType=1）此前若显式传了
  Encoder，改造后必须改为给该轴挂一个 IAuxEncoder 子设备；未挂载时调用会抛
  InvalidOperationException，不再静默锁到 0 号通道。软件锁存（LatchType=0，默认）不受影响。
  实现方：框架内 BaseAxisDevice/BaseAuxEncoder 已同步（PF.Infrastructure 1.0.19）；项目里若
  自写了 IAxis 实现或覆写了这三个方法，必须删掉多余的参数才能编译。

v1.0.19 (2026-09-14) — 辅助编码器补齐位置读回（含破坏性变更）+ 调试视图导航常量

[新增]
- IMotionCard.GetExtraPos(int Channel, double Mulit = 2, CancellationToken token = default)：
  读取辅助编码器通道当前位置，与既有 SetExtraPos 对称（板卡 SDK 早就有对应的
  dmc_get_extra_encoder，只是此前一直没接进契约）。
- IAuxEncoder.GetPositionAsync(CancellationToken token = default)：对上面这个板卡级原语的
  设备层封装，业务代码/调试页读位置走这个，不必关心是不是挂了轴。
- NavigationConstants.Views.AuxEncoderDebugView：辅助编码器调试视图的导航键。

[破坏性变更 — IMotionCard / IAuxEncoder]
- 两个接口均新增上述方法。【调用方】不受影响。【实现方】框架内 BaseMotionCard/LTMDCMotionCard
  与 BaseAuxEncoder 已同步（PF.Infrastructure 1.0.18）；项目里若自写了 IMotionCard 或
  IAuxEncoder 实现，必须补齐新成员，否则不能编译。

v1.0.18 (2026-09-14) — 辅助编码器抽取为独立设备契约（含破坏性变更）

[新增]
- IEncoderCarrier（PF.Core.Interfaces.Device.Hardware.Card）：辅助编码器可挂载的宿主能力接口，
  只表达"能提供辅助编码器通道原语操作的底层板卡"。由 IMotionCard 直接实现（EncoderCard 返回
  自己）与 IAxis 实现（EncoderCard 转发到已挂载的 ParentCard）。
- IAuxEncoder（PF.Core.Interfaces.Device.Hardware.Encoder.Basic）：独立的辅助编码器设备契约，
  挂载父设备类型为 IEncoderCarrier —— HardwareConfig.ParentDeviceId 填运动控制卡 DeviceId
  即直接挂在卡下单独使用，填某根轴的 DeviceId 则挂在该轴下随轴分组编排；两种拓扑最终都落到
  同一块物理板卡的 SetExtraPos / SetLtcLatchMode 等通道级原语，与线扫相机"挂/不挂采集卡"是
  同一思路的镜像。成员：Channel（通道号）、Multiplier（倍率）、SetPositionAsync /
  SetLatchModeAsync / GetLatchNumberAsync / GetLatchPositionAsync。
- HardwareCategory 新增 AuxEncoder。
- AlarmCodes.Hardware 新增 AuxEncoderCardDisconnected（HW_ENC_001，编号 50026）。

[破坏性变更 — IMotionCard / IAxis]
- IMotionCard : IHardwareDevice → IMotionCard : IEncoderCarrier；
  IAxis : IHardwareDevice → IAxis : IEncoderCarrier。
  两者均因此新增 IMotionCard EncoderCard { get; } 成员（继承自 IEncoderCarrier）。
- 【调用方】仅经接口调用既有运动/轴方法的代码不受影响，源码级兼容。
- 【实现方】框架内 BaseMotionCard（EncoderCard => this）与 BaseAxisDevice（EncoderCard 转发到
  ParentCard）已同步（PF.Infrastructure 1.0.17）；项目里若自写了 IMotionCard 或 IAxis 实现
  （未继承 BaseMotionCard/BaseAxisDevice），必须补齐 EncoderCard 成员，否则不能编译。

[废弃]
- IAxis.SetExtraPos 标记 [Obsolete]：辅助编码器操作请改为在硬件配置中新增一个 IAuxEncoder
  （ParentDeviceId 指向本轴或所在运动控制卡），调用其 SetPositionAsync。方法本身行为不变
  （仍委托给 ParentCard.SetExtraPos），仅为迁移期兼容保留，将在后续版本移除。
  IMotionCard.SetExtraPos 与 SetLatchMode/GetLatchNumber/GetLatchPos 的硬件锁存分支
  （Encoder 参数）不受影响——它们本就是板卡级原语，IAuxEncoder 的新方法最终也委托到这里。

[为什么这么改]
- SetExtraPos（Channel+Pos+Mulit）本质是板卡通道级操作，与"是不是某根轴"无关，此前借道 IAxis
  暴露纯属历史遗留，导致想用辅助编码器就必须先挂一根轴。抽成独立设备后，产线跟随编码器、
  外部测长轮等与具体伺服轴无关的场景不必再假借某根轴才能访问；确实要"挂在某根轴下"分组
  编排的场景，通过 IEncoderCarrier 这层能力接口原样保留，无需额外的挂载逻辑分支。

v1.0.17 (2026-09-10) — 补齐 IAxis.GetLatchPos 的辅助编码器通道号（含破坏性变更）

[破坏性变更 — IAxis]
- GetLatchPos 新增 int Encoder = 0（插在 LatchType 与 token 之间），与 1.0.16 给
  SetLatchMode / GetLatchNumber 加的那个参数同一含义、同一位置。1.0.16 漏了这一个方法：
  设置和读计数都已经能指定辅助编码器通道，唯独读锁存位置还锁死在轴号上，
  三个方法凑不成一套完整的硬件锁存流程。
- 【调用方】新参数带默认值，且既有调用点都是按名字传 token 的，源码级兼容、重编译即可。
- 【实现方】框架内 BaseAxisDevice 已同步（PF.Infrastructure 1.0.15）；项目里若自写了 IAxis
  实现、或覆写了这个方法，必须补齐新参数，否则不能编译。

v1.0.16 (2026-09-10) — IAxis 锁存接口新增辅助编码器通道号（含破坏性变更）

[破坏性变更 — IAxis]
- SetLatchMode 新增 int Encoder = 0（插在 LatchType 与 token 之间）：硬件锁存
  （LatchType=1）要指定的是"锁存哪一路辅助编码器"，此前这个位置实际传的是轴号，
  多轴卡上轴号与编码器通道号并不总是一一对应，锁到的常常不是想要的那一路。
- GetLatchNumber 同样新增 int Encoder = 0（插在 LatchType 与 token 之间）——读锁存计数
  必须与设置时用的是同一路编码器，否则读到的是别路的计数。
- 【调用方】新参数带默认值，且既有调用点都是按名字传 token 的，源码级兼容、重编译即可；
  若有把 token 按位置紧跟在 LatchType 后面的写法，需要改成命名参数。
- 【实现方】框架内 BaseAxisDevice 已同步（PF.Infrastructure 1.0.14）；项目里若自写了 IAxis
  实现、或覆写了这两个方法，必须补齐新参数，否则不能编译。

v1.0.15 (2026-08-28) — 界面操作日志契约：两个特性 + 跨层开关接口

[新增]
- OperationLogKeyCatalogAttribute（Attributes/）：类级，标记"该静态类是操作日志键目录"——
  内层嵌套静态类名 = 页面名、public const string 字段 = Key、DescriptionAttribute = 默认描述，
  由 UI 层的 OperationLogKeyRegistry 反射扫描发现，用法对齐 [ModuleNavigation] 的注册套路。
- OperationLogCriticalAttribute（Attributes/）：字段级，标记某 Key 属于"关键操作"
  （启停/复位/保存/删除/确认/登录登出/下发指令等会改变状态或有留痕价值的动作），默认启用
  记录；未标记的键默认不启用，由项目级配置按需打开。
- IOperationLogSettings（Interfaces/Configuration/）：bool EnableOperationLog 总开关的跨层
  接口——UI 基础设施层（02 层）不允许引用应用层（06 层），开关经此契约下探。


		v1.0.14 (2026-08-24) — 光源控制器通道数可配置（含接口破坏性变更）

		[破坏性变更 — ILightController]
		- 新增 int ChannelCount { get; }：光源通道数，注册硬件时由 ConnectionParameters["ChannelCount"]
		  指定，不写则为 4。【实现方】框架内三个实现（CTS/HikCom/OPT LightController，见
		  PF.Infrastructure 1.0.12）已同步；项目里若自写了 ILightController 实现，必须补齐该成员，
		  否则不能编译。调用方不受影响。

		[为什么加这个成员]
		- 光源控制器调试页此前把通道数写死成 4（LightValue1~4 四个固定属性 + XAML 四行滑块），
		  不同厂商/型号实际通道数并不一致，界面要么凑不够、要么用不完。通道数纳入契约后，
		  调试页改为按设备实际 ChannelCount 动态渲染（见 PF.Modules.Debug 1.0.10）。

		v1.0.13 (2026-08-21) — 视觉调试契约解耦（含接口破坏性变更）

		[破坏性变更 — IVisionContextManager]
		- 新增 IVisionService? TryGet(EngineMode mode)：取已拉起的引擎，未拉起或已释放返回 null，
		  与 GetOrCreate 的区别是绝不创建。
		- 【实现方】框架内唯一实现 PF.Vision.Halcon 的 VisionContextManager 已同步；项目里若自写了
		  IVisionContextManager 实现，必须补齐该成员，否则不能编译。调用方不受影响。

		[破坏性变更 — IHalconDebugService]
		- 新增 string ProcedureDirectory { get; }：过程文件所在目录（已规范化的完整路径）。
		  同样只影响自写实现方，调用方不受影响。

		[为什么加这两个成员]
		- ProcedureDirectory：GetProcedureSignatureAsync 原先为了取这一个字符串而 GetOrCreate(Debug)，
		  于是生产机上每次开机凭空多出一个永不使用的 HDevEngine。目录暴露出来后，签名解析、过程枚举、
		  HDevelop 启动参数三处彻底不碰引擎。
		- TryGet：IsDebugServerActive 原先缓存在本地字段里，调用方 ReleaseAsync(Debug) 释放引擎后仍报
		  true。改为以引擎为唯一真相后，需要一个"不创建引擎"的取法——属性 getter 里既不能建引擎，
		  也不该靠类型 cast 兜底。

v1.0.12 (2026-08-18) — 新增串口调试视图导航常量

[新增]
- NavigationConstants.Views.SerialPortDebugView。
  PF.Infrastructure 的 SerialPortCommunication 用它打 [CommunicationUI] 特性，
  PF.Modules.Debug 用它注册导航，两边必须引用同一个常量，故放在 PF.Core。
  纯新增，无破坏性变更。

v1.0.11 (2026-08-18) — ILightController 光源亮度读写完善（破坏性变更）

[破坏性变更 — ILightController]
- SetLightValue(int Channel, int LightValue)
  → SetLightValue(int Channel, int LightValue, CancellationToken token = default)。
  调用方源码级兼容（新参数带默认值，既有调用点不必改）。
- 新增 Task&lt;int&gt; GetLightValue(int Channel, CancellationToken token = default)。
  调用方不受影响。
- 【实现方】以上两项都要跟：框架内 BaseLightController 抽象声明与 CTS/OPT/海康实现已同步
  （PF.Infrastructure 1.0.10），项目里自写的光源控制器子类必须补齐 SetLightValue 的新签名、
  并新增 GetLightValue 的 override，否则不能编译。

[语义约定]
- GetLightValue 读取失败【抛异常】，不要返回 0。返回 0 会让"亮度确实是 0"与"根本没读到"
  在调用方看来完全一样，界面会把滑块悄悄归零、误导现场以为光源已关。
- SetLightValue 反过来，失败【不外抛】只记 Error 日志：它在工站流程里被直接 await，
  抛出会把整条产线带停。

v1.0.10 (2026-08-17) — 线扫视觉契约（线阵相机 / 图像采集卡 / GenICam 节点通道）+ IAttachedDevice 泛化（含破坏性变更）

[破坏性变更 — IAttachedDevice]
- 挂载契约由"只能挂运动控制卡"泛化为"可挂任意父设备"（线阵相机需要挂在图像采集卡下，
  原接口把父设备写死成 IMotionCard，新增一种宿主就得改服务层）：
  · IMotionCard? ParentCard          → IHardwareDevice? ParentDevice
  · void AttachToCard(IMotionCard)   → bool TryAttachTo(IHardwareDevice)
    （父设备类型与本设备期望不符时返回 false 由调用方记警告，不抛异常——配置写错父设备
      指向不该让整机初始化中断）
- 新增泛型 IAttachedDevice&lt;TParent&gt; : IAttachedDevice（Parent 属性 + AttachTo(TParent)），
  子设备实现它来声明自己期望的父设备类型（轴/IO → IMotionCard，线阵相机 → IFrameGrabberCard），
  服务层只依赖非泛型接口，今后再加宿主种类不必再改注入点。
- 影响面：
  · 只继承 BaseAxisDevice / BaseIODevice 的项目设备类【无需改动】——两个基类已保留
    ParentCard / AttachToCard 领域别名转发到 Parent / AttachTo（见 PF.Infrastructure 1.0.9）。
  · 直接实现 IAttachedDevice 的自定义设备类需改为继承 AttachedDeviceBase&lt;TParent&gt;
    或自行补齐 ParentDevice / TryAttachTo。
  · 直接读 device.ParentCard 的调用点（如自写的硬件树/调试页）需改读 ParentDevice。

[新增 — 线阵相机契约]
- ILineScanCamera（PF.Core.Interfaces.Device.Hardware.Camera.LineScan）：与 IIntelligentCamera
  刻意不复用同一接口——智能相机是"触发→相机内部跑算法→返回结果串"，线阵相机吐的是原始图像、
  算法在上位机侧。成员：ApplyConfigAsync（整份配置下发，null 字段视为不下发）/ ArmAsync（开流并
  武装，必须早于轴运动）/ StopAsync / SoftwareTriggerFrameAsync / WaitFrameAsync（主动等一帧，
  超时抛 TimeoutException）/ FrameReceived 事件（连续取流预览，在 SDK 回调线程触发）/
  SaveLastImageAsync / DiscoverAsync；只读态 ModelName、SerialNumber、TransportLayer、
  HasFrameGrabber、LineSpacingUm、IsGrabbing、LastImage*。
  接口【不引用 IAxis】：线扫的第二个维度由运动提供，但"轴与相机的时序编排"属机构层职责。
- IFrameGrabberCard（...Device.Hardware.Card）：与 IMotionCard 并列的宿主类设备。职责边界——
  采集卡管"流怎么切帧"（帧长/帧起点/帧超时），相机管逐行曝光（行触发与编码器始终配在相机上）。
  成员：ApplyFrameControlAsync / SoftwareTriggerFrameAsync / DiscoverCamerasAsync。
  拓扑约定：卡的 ParentDeviceId 为空（第 1 层），相机的 ParentDeviceId 填卡的 DeviceId（第 2 层）；
  相机 ParentDeviceId 留空即 GigE/USB 直连、帧控制回落到相机自身节点树——配置拓扑本身表达链路
  类型，不再需要额外的"链路开关"字段。
- IGenICamNodeAccess：相机与采集卡共用的 GenICam 节点通用读写通道（IsNodeAvailableAsync /
  GetNodeAsync / SetNodeAsync / GetEnumEntriesAsync / ExecuteCommandAsync / EnumerateNodesAsync）。
  存在意义：可配节点因型号、固件而异（同为采集卡，CameraLink 卡有 ImageHeight/FrameTimeoutTime，
  CXP 卡却是 IspGamma/BayerCFAEnable），强类型配置覆盖不到的部分走本通道，换型号改配置即可，
  不必改代码重新发版。值一律以字符串传递（枚举用 symbolic 名、数值用不变文化格式）。
  ⚠️ EnumerateNodesAsync 是主动扫描，节点数可达上千、耗时秒级，只应由用户显式触发，勿放轮询里。

[新增 — 实体 PF.Core.Entities.Hardware.Vision]
- ScanProfile：线扫扫描配方，把"轴怎么走"和"相机怎么拍"绑在一起。线扫相机单独配是配不出正确
  图像的——帧长取决于行程与行间距，行频取决于速度与行间距，曝光上限又反受行频限制，任一项填错
  症状都是"图不对"但原因分散在轴/相机/光学。本类集中换算与约束：Direction / ScanLengthMm /
  TheoreticalAccel|DecelDistanceMm / EffectiveApproach|OvertravelMarginMm / MoveStartMm / MoveEndMm /
  FrameHeightLines / ActualLineRate / MaxExposureTimeUs / EstimatedFrameTimeMs / FrameTimeoutMs，
  并由 Validate() 在扫描【开始前】拦住非法组合。
- LineScanCameraConfig / LineTriggerConfig / EncoderConfig / FrameControlConfig：相机本体参数、
  行触发、编码器、帧控制四组配置。
- LineScanFrame（一帧图像及其元数据）/ GenICamNode（节点名/分类/类型/权限/当前值）/
  DiscoveredDeviceInfo（枚举到的在线设备）。

[新增 — 枚举 PF.Core.Enums.Hardware.Vision]
- CameraTransportLayer / LineTriggerMode / ImagePixelFormat / ImageFileFormat /
  GenICamNodeType / GenICamAccessMode。

[新增 — 常量]
- HardwareCategory 新增 FrameGrabber（追加在枚举末尾，既有成员数值不变）。
- NavigationConstants.Views 新增 LineScanCameraDebugView / FrameGrabberDebugView。
- AlarmCodes.Hardware 新增三条：HW_CAM_003 线阵相机取帧超时（50010）、
  HW_FG_001 采集卡打开失败（50011）、HW_FG_002 采集卡连接中断（50012），均带 SOP 排故步骤。

v1.0.9 (2026-08-03) — 配置路径按项目隔离（破坏性变更，升级前必须执行现场迁移）

[破坏性变更 — 配置路径]
- ConstGlobalParam.ConfigPath 由固定的 D:\PFConfig\PFAutoFrameWork 改为
  D:\PFConfig\PFAutoFrameWork\{项目名}\。新增 ConfigRoot（共享根目录）、ProjectName、
  IsInitialized、Initialize(string) 四个成员。
- ConfigPath 在未调用 Initialize 时【直接抛 InvalidOperationException】，不回退根目录：
  漏调初始化的宿主必须在首次运行时暴露，而不是静默地把配置写到共享根目录。
  WPF 主程序由 PFApplicationBase 构造函数自动完成，项目侧无需改动；自建宿主
  （工具、测试宿主）必须显式调用 Initialize。
- 因此【禁止】在静态字段初始化器中捕获 ConfigPath，否则会先于 Initialize 求值并抛
  TypeInitializationException。本次已将 SecsGemSystemParam.filepath 与
  ValidateConfiguration.filePath 由静态字段改为表达式属性。

[背景]
AppParamDbContext.EnsureDefaultParametersCreatedAsync 会删除"不在本项目默认参数集里"
的参数行（废弃参数清理）。同一台机器上换项目运行时，A 项目的参数在 B 项目眼里全是废弃行，
参数/用户/硬件/通讯四张表会被整批清空。Recipe\、AxisPoints\、user.config 等同样存在
同名互相覆盖问题。

[升级须知]
已部署机台必须先执行 Installer\Migrate-Config.ps1 将旧布局搬到项目子目录，
详见 Installer\现场升级迁移说明.md。

v1.0.8 (2026-07-21) — 新增 HardwareUIAttribute（硬件调试界面路由特性）

[新增]
- HardwareUIAttribute：与 CommunicationUIAttribute/StationUIAttribute 同一约定，供 HardwareDebugView
  按设备运行时类型反射发现自定义调试视图（ViewName），作为项目自定义硬件设备调试页的路由扩展点。
  纯新增、无破坏性变更；配合 PF.Modules.Debug 1.0.6 的硬件调试路由改造使用。

v1.0.7 (2026-07-17) — Modbus 主站契约补强：自动重连 + 可取消连接 + 原始报文透传（含破坏性变更）

[破坏性变更]
- IModbusRtuMaster/IModbusTcpMaster 新增 AutoReconnect（意外断开后是否自动重连）与
  ReconnectIntervalMs（重连间隔）两个属性；IModbusTcpMaster.ConnectAsync 签名新增
  可选 CancellationToken 参数（此前无 token 的连接尝试在目标不可达时按 OS 默认超时
  阻塞约 21s，外部编排无法打断）；IModbusMaster 新增 SendRawAsync（原始 PDU 透传）
  与 BuildFrame（构建完整请求帧供预览）两个方法；IModbusTcpMaster 新增
  TransactionIdAutoIncrement（MBAP 事务号是否逐请求递增）、FixedTransactionId（固定模式下
  使用的事务号）属性与 ResetTransactionId() 方法。下游若有这些接口的自定义实现类需补齐成员后才能编译；框架内置实现
  （PF.Infrastructure 1.0.8 的 ModbusRtuMaster/ModbusTcpMaster）已同步，仅经接口
  调用的代码源码级兼容、重编译即可。

[新增]
- IModbusMaster.SendRawAsync：发送原始 PDU（功能码+数据，1~253 字节）并等待一帧响应，
  返回原始响应 PDU。与 8 个内置读写方法不同——不校验功能码回显，也不把从站异常响应
  解包成 ModbusException，异常响应原样返回（首字节最高位置 1）。用于框架未覆盖的
  功能码或自定义诊断报文。
- IModbusMaster.BuildFrame：构建与实际发送帧逐字节一致的完整请求帧（RTU 含从站地址
  +CRC16，TCP 含 MBAP 头，TransactionId 以 0x0000 占位），仅供报文预览/记录，不产生 IO。
- IModbusTcpMaster.TransactionIdAutoIncrement/FixedTransactionId/ResetTransactionId：MBAP
  事务号是否逐请求递增（默认 true）、固定模式下使用的事务号、重置计数器；固定事务号模式
  （FixedTransactionId）用于兼容个别按固定事务号应答的网关或与抓包比对（会削弱靠事务号
  识别迟到/串包响应的能力，仅建议调试场景）。

v1.0.6 (2026-07-16) — 新增 Modbus 主站通讯契约

[新增]
- ICommunication 通讯管理框架新增 Modbus 支持：IModbusMaster（共享操作契约，8 个功能码
  01/02/03/04/05/06/0F/10）+ IModbusRtuMaster/IModbusTcpMaster（分别对齐 ISerialCommunication/
  IClient 的既定拆分方式，只做 Master/客户端角色）。
- CommunicationCategory 新增 Modbus 成员。
- 新增 ModbusExceptionCode 枚举（标准异常码 0x01~0x0B）与 ModbusException（携带 UnitId/
  FunctionCode/ExceptionCode），从站返回异常响应时抛出。

v1.0.5 (2026-07-15) — 锁存接口 LtcMode 默认值对齐（行为变更）

[行为变更]
- IAxis.SetLatchMode 与 IMotionCard.SetSoftWareLatchMode/SetLtcLatchMode 的可选参数
  LtcMode 默认值由 0 改为 1，与实现层（BaseAxisDevice/LTDMCMotionCard 均默认 1）及
  现有生产调用对齐——此前经接口调用与经类调用会取到不同默认值（C# 可选参数按调用点
  静态类型在编译期固化）。下游若有经接口调用且省略 LtcMode 的代码，重编译后默认值
  从 0 变为 1；显式传参的调用不受影响。

v1.0.4 (2026-07-09) — SecsGemNodeMessage 大端字节序统一 + DataType 格式码修正（含破坏性变更）

[破坏性变更]
- DataType.I1（原值 6，非标准格式码）改为 0b01100100（0x64）；DataType.I2（原值
  0b011000100，9 位数值已溢出字节范围）改为 0b01101000（0x68）。均修正为符合 SECS-II
  标准的格式码——此前两个值本身就是错误的，用其构造/解析的 I1/I2 报文与遵循标准协议的
  对端不兼容；修正后线格式才正确，但会改变此前"错误但已在跑"的现场行为，故按破坏性处理。

[修复]
- SecsGemNodeMessage 构造函数重写（SecsGemNodeMessage.cs）：
  - F4/F8/I8(long) 分支此前遗漏大端转换，浮点与 long 值以主机序（小端）直接写入 Data，
    对端解析出乱值；现与 I2/I4/U2/U4/U8 一致统一转大端序。
  - I4 分支此前仅匹配 `is Int64`，传入最自然的 `int` 完全不命中；命中时又把 8 字节数据
    错误标记为 4 字节 I4 类型，对端按声明长度解析会错位。改用 Convert.ToInt32 统一接受
    任意可转换的数值输入。
  - 构造函数末尾的防呆校验此前是死代码（SubNode 有属性初始化器恒不为 null，判断条件
    永假），类型不匹配时会静默产出 Data=null 的空节点，序列化后对端收到长度为 0 的字段
    且无任何报错；现改为在数据未成功构造时正确抛出 ArgumentException。
  - 明确约定并落地：SecsGemNodeMessage.Data 一律存放大端序（SECS-II 线格式）字节，
    序列化器原样输出、解析器不再修改 Data（配合 PF.Infrastructure 同版本的解析器/
    VID 更新器修复，见该包 changelog）。

v1.0.3 (2026-07-07) — 通讯契约 + HALCON 视觉数据模型 + 新增报警码

[新增]
- ICommunication/ICommunicationManagerService 契约、CommunicationCategory/Role 枚举、
  CommunicationUIAttribute；FileTransfer 传输通道全套接口/实体/事件；ISerialCommunication。
- HALCON 视觉：EngineMode/RoiOp/RoiType/VisionRoiConfig/ProcedureSignature 等枚举与接口。
- AlarmCodesExtensions 新增 Detection.ResumeException（编号 50202）。

v1.0.2 (2026-06-17) — 报警系统 SECS/GEM 支持 + SecsGemNodeMessage 修复

[破坏性变更]
- AlarmInfoAttribute 构造函数新增三个必填参数（签名已变更，现有标注须更新）：
    messageEn     — 英文报警描述，上传 SECS/GEM S5F1 消息用，限 35 字符
    messageID     — 整型报警 ID，框架内置从 50000 起步，需开发者保证唯一性
    messageIDHex  — 英文报警文本（限 40 字符），与 messageEn 保持一致
  旧签名：(category, message, severity, solution, imagePath?)
  新签名：(category, message, messageEn, severity, solution, messageID, messageIDHex, imagePath?)

[新功能]
- AlarmInfoAttribute 新增属性：MessageEn、MessageID、MessageIDHex。
- AlarmInfo 模型新增属性：MessageEn、MessageID、MessageIDHex，
  报警字典查询结果完整携带 SECS/GEM 上传元数据。
- AlarmRecord 模型新增属性：MessageEn、MessageID、MessageIDHex，
  触发记录完整携带上传所需字段，无需在上传侧二次查字典。
- AlarmCodes 内置 26 条框架报警全量更新 [AlarmInfo] 特性：
    HW 类（硬件）：ServoError=50000 … MonitorFailure=50025
    SYS 类（系统）：InitializationTimeout=50016 … UndefinedStep=50021
    SAFE 类（安全）：SafeDoorOpen=50022 … MonitorFailure=50025
  所有英文描述均控制在 35 字符内，符合 SECS/GEM ALID 上报规范。

[Bug 修复]
- SecsGemNodeMessage.SetValue(DataType.U4)：
  将 `if (_value is uint uU4)` 强类型守卫改为 Convert.ToUInt32(_value)，
  修复 int / long / short 等整型赋值时因类型不匹配而静默跳过、Data 字段保持旧值的问题。
  TypedValue 由 uU4 改为存储原始 _value，与其他 DataType 分支行为一致。

v1.0.1 - 框架初始独立版本
		</PackageReleaseNotes>
		<Description>PF.AutoFramework 核心契约层：接口、枚举、特性，无任何外部依赖</Description>
		<GeneratePackageOnBuild Condition="'$(Configuration)' == 'Release'">true</GeneratePackageOnBuild>
	</PropertyGroup>
</Project>
