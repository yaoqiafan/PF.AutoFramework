<Project Sdk="Microsoft.NET.Sdk">

	<Import Project="..\Common.props" Condition="Exists('..\Common.props')" />

	<ItemGroup>
		<PackageReference Include="log4net" />
		<PackageReference Include="Stateless" />
		<PackageReference Include="NPOI" />
		<PackageReference Include="System.Drawing.Common" />
		<PackageReference Include="System.IO.Hashing" />
		<PackageReference Include="System.IO.Ports" />
	</ItemGroup>


	<ItemGroup>
	  <ProjectReference Include="..\PF.Core\PF.Core.csproj" />
	  <ProjectReference Include="..\PF.SecsGem.DataBase\PF.SecsGem.DataBase.csproj" />
	</ItemGroup>

	<ItemGroup>
	  <Reference Include="MvCodeReaderSDK.Net">
	    <HintPath>..\DLL\MvCodeReaderSDK.Net.dll</HintPath>
	  </Reference>
	  <Reference Include="Keyence.AutoID.SDK">
	    <HintPath>..\DLL\Keyence.AutoID.SDK.dll</HintPath>
	  </Reference>
	  <Reference Include="Communication">
	    <HintPath>..\DLL\Communication.dll</HintPath>
	  </Reference>
	  <!-- 海康工业相机 SDK（MVS）托管封装 V2。取自 SDK 安装目录 Development\DotNet\AnyCpu\netstandard2.0。
	       ⚠️ 部署要求：托管层内部仍会 P/Invoke 原生 MvCameraControl.dll，
	       需将 SDK 的 Libraries\win64 下原生库一并复制到程序输出目录；
	       CameraLink/CXP/XoF 链路还需另行安装采集卡驱动(MVFG)。 -->
	  <Reference Include="MvCameraControl.Net">
	    <HintPath>..\DLL\MvCameraControl.Net.dll</HintPath>
	  </Reference>
	</ItemGroup>

	<PropertyGroup>
		<IsPackable>true</IsPackable>
		<Version>1.0.10</Version>
		<PackageReleaseNotes>
v1.0.10 (2026-08-18) — 新增海康串口光源控制器 + 光源亮度读写完善

[新增]
- HikComLightController（海康串口光源控制器，基于 ISerialCommunication）：指令格式
  写 S{通道字母}{4 位亮度}#、读 S{通道字母}#，通道 1~4；健康检查按串口连接状态走
  AlarmCodes.Hardware.LightControllerError，恢复后自动清警。
  类型访问性为 public（与 CTSLightController 一致），消费项目可直接在硬件工厂中实例化；
  构造参数 ISerialCommunication 由项目侧按实际串口配置注入。
- BaseLightController.GetLightValue 抽象成员；CTSLightController / HikComLightController
  给出实现，OPTLightController 仍为未实现占位。
- CtsAPI.GetDigitalValue：启用此前被注释掉的 P/Invoke 声明（ControllerDll.dll）。

[破坏性变更]
- BaseLightController 的 SetLightValue 抽象签名新增 CancellationToken token = default，
  并新增抽象成员 GetLightValue（契约变更见 PF.Core 1.0.11）。CTS / OPT / 海康三个实现已同步；
  项目自写的光源控制器子类需同步修改 override 签名并补齐 GetLightValue 后才能编译。

[修复 — 关键，海康串口光源]
- 读写抢应答：读(SA#)与写(SA0500#)共用 lightEvent + receivestr 这一对字段却互不排斥，
  并发时一条应答会同时唤醒两个等待方，读到的可能是写的回执、写等到的可能是读的回复。
  现整笔"发送→等应答"事务由信号量串行化。
- 应答解析恒失败：读回来的是带帧字符串（形如 SA0500#，可能还跟 CR/LF），
  对整串做 int.TryParse 必然失败，真机上读取恒不成功。改为按"取应答中的数字段"解析，
  裸数字与带帧两种回复都能认。
- 超时留孤儿等待：原写法起两个任务（一个等事件、一个 Thread.Sleep(3000)）比谁先到，
  一笔事务占两个线程池线程；且超时胜出后，等事件那个任务并不会消失，它继续挂着，
  下一笔应答到达时被唤醒并吃掉——错位一次之后每次读写都是错的。
  改用 ManualResetEventSlim.Wait(超时, 令牌) 单线程等待，超时与取消一次覆盖，不留孤儿。
- 重连后回调重复订阅：InternalConnectAsync 里只有 DataReceived += 没有 -=，
  断开再连接会挂多份回调。现改为先减后加，并在 InternalDisconnectAsync 中退订。
- 设置亮度失败的日志级别由 Debug 改为 Error（原先实际等于看不见）。

[变更]
- CTSLightController / HikComLightController 的 GetLightValue 读取失败抛异常、不返回 0，
  理由见 PF.Core 1.0.11。该方法为本次新增、无既有调用方，不影响现网代码。
- 清理 CTSLightController / HikComLightController 中误自动引入的
  using static NPOI.HSSF.Util.HSSFColor。

v1.0.9 (2026-08-17) — 海康线阵相机 / 图像采集卡接入 + 线扫检测模组 + 挂载基类下沉

[部署要求 — 先看这条]
- 本版新增对海康工业相机 SDK（MVS）托管封装 MvCameraControl.Net.dll 的引用（编译期引用随包
  分发的 DLL 目录，不打进 nupkg，与既有 Communication.dll / Keyence SDK 同一处理方式）。
  运行现场还需：① 把 SDK Libraries\win64 下的原生库复制到程序输出目录（托管层内部仍 P/Invoke
  原生 MvCameraControl.dll）；② CameraLink/CXP/XoF 链路另装采集卡驱动 MVFG。
  不接线扫的项目不受影响——设备不在硬件配置里就不会被实例化，也不会去加载 SDK。

[新增 — 挂载基类]
- AttachedDeviceBase&lt;TParent&gt; : BaseDevice, IAttachedDevice&lt;TParent&gt;（PF.Infrastructure.Hardware）：
  统一实现 Parent / AttachTo / TryAttachTo（类型不匹配返回 false 不抛异常），子类只需按需重写
  protected virtual OnAttached(TParent) 记日志或做初始化。对应 PF.Core 1.0.10 的契约泛化。
- BaseAxisDevice / BaseIODevice 改为继承 AttachedDeviceBase&lt;IMotionCard&gt;，并保留
  ParentCard（=&gt; Parent）与 AttachToCard(card)（=&gt; AttachTo）两个领域别名——既有工站/机构
  代码与日志语义完全不变，重编译即可，无需改动调用点。

[新增 — 线阵相机]
- BaseLineScanCamera : AttachedDeviceBase&lt;IFrameGrabberCard&gt;, ILineScanCamera：线阵相机公共基类，
  HasFrameGrabber 直接由 Parent 是否为空判定（挂卡=经采集卡取流，未挂=直连），
  GenICam 节点访问与帧事件分发在基类内收敛，厂商子类只补 SDK 相关部分。
- HikLineScanCamera：海康线阵相机实现。按序列号优先、枚举索引兜底选定设备；
  ApplyConfigAsync 一次下发相机本体参数 + 行触发 + 编码器（单个节点失败只记警告不中断整体）；
  ArmAsync 开流武装、WaitFrameAsync 主动等帧、FrameReceived 连续预览；
  取帧超时按 HW_CAM_003 报警；SaveLastImageAsync 支持存盘；DiscoverAsync 枚举在线相机。
  SDK 图像缓存节点数（ImageNodeNum）做成可配置项——占用 ≈ 单帧字节数 × 节点数，线扫大帧
  （16K 宽 × 上万行可达数百 MB）下必须按实际帧长下调；小于 2 按 2 处理。
- 帧控制策略对象 IFrameControl + DeviceFrameControl / InterfaceFrameControl：帧长与帧触发
  该写到相机节点树还是采集卡节点树，由链路形态在连接时选定，业务侧无感。

[新增 — 图像采集卡]
- BaseFrameGrabberCard : BaseDevice, IFrameGrabberCard（GenICam 通道在基类实现）。
- HikFrameGrabberCard：海康采集卡实现。序列号/索引选定；ApplyFrameControlAsync 下发帧长、
  帧超时、帧触发源、残帧策略等（≤0 的字段视为不下发、沿用当前值）；SoftwareTriggerFrameAsync
  按最近一次下发的触发源自动选用 StreamSoftwareTrigger / QuickSoftwareTrigger0；
  DiscoverCamerasAsync 枚举挂在本卡上的相机。打开失败 / 连接中断分别对应 HW_FG_001 / HW_FG_002。

[新增 — GenICam 支撑（Hardware\Vision\Hikvision）]
- GenICamNodeAccessor：节点读写/枚举项/命令执行/全量节点扫描的统一实现，相机与采集卡共用。
- MvTypeMapper：SDK 节点类型与框架 GenICamNodeType / GenICamAccessMode 的映射。
- GenICamNodeGlossary：常用节点名的中文释义与分类表，供调试面板的属性树直接显示——
  现场排查最花时间的往往是先猜出节点叫什么、以及它此刻是不存在还是存在但不可写。
- MvSdkLifetime：SDK 初始化/反初始化引用计数，多设备共存时不会被先关闭的那个拖垮。

[新增 — 线扫检测模组]
- LineScanDetectionModule : BaseMechanism（标注 [MechanismUI("线扫检测模组",
  "LineScanDetectionModuleDebugView", 20)]，调试页随 PF.Modules.Debug 1.0.7 一起提供）。
  框架级可复用模组：扫描轴与相机的 DeviceId 由注册处传入（不像项目自写模组那样写死），
  一台设备多条扫描线时注册多个实例、各用不同 serviceKey 即可，模组调试面板会全部列出。
  ScanAsync(profile, baseConfig, token) 完成一次完整扫描：校验配方 → 移到起点前的加速余量位 →
  开流武装 → 启动扫描运动（不等待）→ 到达扫描起点时发帧触发 → 等一帧 → 等轴走完 → 停流。
  开流必须早于轴运动（否则丢起始行），扫描区必须完整落在匀速段内（加减速段留在余量里）。
  编码器假定直连相机 IO（非从轴卡取信号），故模组只保证"匀速走过扫描区"，不参与逐行同步。

v1.0.8 (2026-07-17) — Modbus 主站健壮性修复合集 + 断线自动重连 + 原始报文透传

[新增 — 断线自动重连]
- ModbusRtuMaster/ModbusTcpMaster 新增 AutoReconnect（默认 true）/ReconnectIntervalMs
  （默认 5000ms）：连接/串口意外失效后按间隔后台重试直至成功，CAS 防重入保证只存在
  一个重连循环；主动 CloseAsync/DisconnectAsync 先取消重连、先置状态再拆连接，不会被
  重连回去。TCP 侧连接失败（含首次 StartAsync）同样进入重连；RTU 侧失效检测发生在
  事务执行/收发出错时，串口空闲期间被拔出要到下一次请求才会被发现。
- Demo Shell 工厂新增可选配置键（缺省保留实现默认值）：TimeoutMs/AutoReconnect/
  ReconnectIntervalMs 两个主站通用；RTU 另支持 Parity/DataBits/StopBits（缺省 N81）——
  此前构造函数支持这三项但工厂不读，遇 8E1 等非默认帧格式从站无法配置。

[修复]
- ModbusRtuMaster 迟到响应污染竞态（v1.0.6 修复的补全）：OnPortDataReceived 的
  _port.Read 此前在 _bufferLock 之外执行，存在"脏字节已读进本地缓冲、尚未进锁，
  新事务在锁内 DiscardInBuffer（驱动缓冲已被读空、丢弃无效）并登记挂起请求，处理
  线程随后进锁把脏字节追加进帧缓冲"的交错，恰好绕过 DiscardInBuffer 防线；读取移入
  同一把锁后两种加锁顺序都安全。
- ModbusRtuMaster 外部取消误报超时：调用方 token 主动取消（如工站取消式暂停）此前
  被包装成 TimeoutException("等待响应超时")，上层会把一次正常暂停当成通讯故障处理；
  现按 OperationCanceledException 原样传播，仅真正的响应超时抛 TimeoutException
  （TCP 侧同步收紧了同一判断）。
- ModbusTcpMaster 帧失步防护：MBAP Length 声明的 PDU 长度超过协议上限 253 视为失步、
  抛 IOException 并废弃连接；帧读取在半途被超时/取消打断（流内残留半帧，后续事务把
  残帧错位解析成 MBAP 头，永远无法重新对齐）时同样主动废弃连接，交给自动重连恢复——
  此前两种情形都会陷入连环超时且无法自愈。
- ModbusTcpMaster MBAP 帧头重新同步：从站多发字节、连接混入非 Modbus 数据、迟到响应残片等
  会让字节流错位，按错位头解析会得到荒诞 Length（实测从站会触发"PDU 长度 24939"这类异常）
  并误判失步断开。改为读 MBAP 头时校验 ProtocolId==0 且 Length∈[2,254]，不满足则滑动窗口
  向前扫描重新对齐（最多 64 字节），就地恢复而不断开；误同步读出的帧会被事务号/从站地址
  匹配兜住不会错当响应。连续滑动仍无合法头才判定严重失步、走断开重连兜底。
- ModbusTcpMaster：ICommunication.StartAsync 此前丢弃编排层传入的 token；ConnectAsync
  新增可选 token 并全程透传（接口签名变化见 PF.Core 1.0.7）。
- ModbusTcpMaster：请求用的流改为拿到请求锁之后再取——等锁期间发生断线重连换流会
  拿到已 Dispose 的旧流；补 ObjectDisposedException 分支翻译成 IOException 维持接口
  契约，不再把原始异常裸抛给调用方。
- ModbusMasterBase 功能码回显集中校验：读响应此前只校验 ByteCount 不校验功能码回显
  （写回显校验里有），长度恰好相同的错帧会被当正常数据接受；异常响应携带的原功能码
  也一并校验，不一致按串包错帧处理而非从站异常。
- ModbusRtuMaster：串口写入设 WriteTimeout=TimeoutMs 兜底（SerialPort 默认无限，硬件
  流控挂死时同步写会永久阻塞且 token 打不断）；写入失败清理挂起请求并触发失效处理。
- TimeoutMs/ReconnectIntervalMs 拒绝 0/负数（0 会让每次请求立即超时，负数会让
  CancellationTokenSource 构造直接抛异常）。

[新增 — 原始报文透传与帧预览]
- IModbusMaster.SendRawAsync/BuildFrame 实现：SendRawAsync 直发原始 PDU、不校验功能码回显、
  从站异常响应原样返回；RTU 因无法预知响应长度改用报文间静默判帧（窗口取 10 个字符时间
  与 30ms 的较大者，低波特率按字符时间放大），响应完整性仍由 CRC 校验兜底，并加最短帧
  防护拦截噪声碎帧；TCP 沿用 MBAP 定界。BuildFrame 供调试面板报文预览复用，预览帧与实发
  帧逐字节一致。
- IModbusTcpMaster.TransactionIdAutoIncrement/FixedTransactionId/ResetTransactionId 实现：MBAP
  事务号计数器改为每实例独立（此前 ModbusTcpFrameCodec 的静态计数器会跨实例共享，无法按实例
  重置）；固定模式下取 FixedTransactionId（volatile int 字段，保证 UI 线程设置与请求线程读取
  间的可见性，读时强转取低 16 位），递增模式原子自增。响应校验始终按本次请求实际发出的事务号匹配。
- ModbusPduCodec 由 internal 改 public（仍在 Modbus.Internal 命名空间，类名访问性提升），
  供调试面板构建标准功能码 PDU 做报文预览——PDU 构建逻辑只此一份，预览与实际发送不跑偏。
- ModbusMasterBase 新增 protected UnknownResponseLength 哨兵常量，ExecuteAsync 收到它表示
  响应长度未知（原始透传），RTU 据此切换为静默判帧、TCP 不受影响。

[行为变更]
- 从站异常响应在 FrameExchanged 事件中按失败事务记录（Success=false、errorMessage
  携带异常码）并记一条 Warn 日志——此前记为成功事务，调试面板/日志上看不出这笔是
  从站拒绝。Success 语义（收到有效的"正常"响应）与事件文档一致。
- RTU 串口意外失效后 Status 置 Error 并触发 Closed("意外断开")——此前 Status 一直
  停留在 Connected，面板显示与实际不符。

v1.0.7 (2026-07-16) — 通讯层统一补齐日志（接入既有 Communication 分类）

[新增]
- TCPClient/TcpServer/SerialPortCommunication/FileTransferChannel/ModbusRtuMaster/
  ModbusTcpMaster 构造函数新增可选 ILogService 参数（缺省 null，不写日志，兼容既有调用点），
  内部统一经 CategoryLoggerFactory.Communication(logger) 包装成 CategoryLogger 使用——
  对齐 BaseDevice/InternalClient/Worker 已有的"构造函数收 ILogService、内部按自身分类
  自包装"惯例，落到 LogConfiguration 里早已注册好的 Communication 专属日志类别/文件
  （此前误用未分类的裸 ILogService 调用，会落进默认分类而非 Communication 分类）。
  此前只有 CommunicationManagerService（编排层）落日志，底层收发实现完全靠事件，
  没人订阅时运行期的断线/超时/协议校验失败在日志文件里没有任何痕迹。
  记录策略：连接/断开/启动/停止等生命周期事件记 Info（TCPClient 的意外断开、
  Modbus 的连接异常断开记 Warn）；异常/失败记 Error 或 Warn 并带上异常对象；
  高频数据不记（TCP 收发字节、Modbus 单次成功事务），避免正常轮询把日志刷屏——
  Modbus 只记失败事务，成功报文的实时查看走已有的 FrameExchanged 事件订阅。
  App.xaml.cs 的 RegisterCommunicationFactories 统一解析一次 ILogService 传给全部工厂。

v1.0.6 (2026-07-16) — 新增 Modbus RTU/TCP 主站实现

[新增]
- PF.Infrastructure.Communication.Modbus：ModbusRtuMaster（直接持有 SerialPort）/
  ModbusTcpMaster（直接持有 TcpClient/NetworkStream），均实现 PF.Core 新增的
  IModbusRtuMaster/IModbusTcpMaster + ICommunication，注册到 CommunicationManagerService。
  协议核心（PDU 构建/解析、CRC16、MBAP 头）下沉到 Internal 命名空间下的
  ModbusPduCodec/ModbusCrc16/ModbusTcpFrameCodec，RTU/TCP 共用，8 个功能码的读写方法
  由内部的 ModbusMasterBase 统一实现，两个传输实现只需各自补一个"发请求收一帧"的
  传输原语。
- ModbusTcpMaster 每次响应校验 MBAP 头 TransactionId/UnitId 与本次请求一致，不匹配
  （网络抖动/上一次超时请求的迟到响应等场景下的串包）直接丢弃、继续等待下一帧。

[修复]
- ModbusRtuMaster.ExecuteAsync：发起新请求前遗漏丢弃串口驱动接收缓冲区里的残留字节——
  上一笔超时请求的迟到响应若恰好在这个窗口到达，会被误当作新事务的响应前缀拼进
  _buffer，轻则 CRC 校验失败报错，重则残片恰好落在"未置异常位"的分支，让本该抛
  ModbusException 的真实异常响应被误判为超时静默吞掉。修正为 DiscardInBuffer() 与
  _buffer.Clear()/挂起请求登记在同一把锁内原子完成。

v1.0.5 (2026-07-15) — 运动/机构/工站/主控并发与契约修复（P1~P3）

[修复 — 关键]
- LTDMCMotionCard.GetSoftWareLatchPos/GetLtcLatchPos：catch 分支 return null 返回的是
  null 的 Task 对象，上层 await 时抛 NullReferenceException（锁存读取失败会以一条
  莫名的 NRE 让寻层扫描失败）；改返回携带 null 值的 Task。
- BaseMasterController.ResetAllAsync：并行复位后补充 hasFailedStation 检查（与
  InitializeAllAsync 对称）。此前子站复位失败被吞掉后主控照常清空活跃报警并回 Idle，
  后续 Start 时故障子站静默不启动，整线"看着在跑"实际缺站。

[修复]
- BaseIODevice.WaitInputAsync：模拟模式由恒返回 false 改为立即满足（与轴设备模拟语义
  一致）；外部取消由返回 false 改为上抛 OperationCanceledException（与
  WaitAxisMoveDoneAsync 对齐——此前用户暂停会被业务层误判为 IO 超时并触发假报警），
  超时才返回 false。
- BaseMasterController.Dispose：补退订 HardwareInputRestored（原仅退订
  HardwareInputTriggered，释放后仍会收到安全门恢复回调）。
- StationBase.ProcessWrapperAsync：派发 Fire(Error) 的 Task.Run 改传
  CancellationToken.None——Pause/Stop 竞态下 token 已取消会导致委托被直接跳过、
  业务异常的报警静默丢失。
- BaseMechanism：InitializeAsync 异常时清 IsInitialized（防复位清警后 CheckReady 放行
  半初始化机构）；_internalHardwares 补并发保护（锁内注册/快照遍历，注册线程与设备
  健康监控线程并发时可能撞车）。
- BaseAxisDevice 模拟保真度：新增虚拟轴位置（绝对运动置目标值/相对运动累加/回零清零），
  CurrentPosition 不再返回随机数（IsAtPoint/IsInSafePosition 在模拟下结果确定），
  GetLatchPos 模拟返回虚拟位置而非 0 哨兵值；全部模拟延时接受取消令牌（模拟运动可被
  暂停/停止打断）。
- SecsGemManger.Dispose：DisconnectAsync 同步等待改 Task.Run 包装（与 TcpServer 同款
  修复），消除有 SynchronizationContext 线程上的死锁风险。

[行为变更]
- BaseMotionCard.SetSoftWareLatchMode/SetLtcLatchMode 抽象声明的 LtcMode 默认值 0→1
  （与 PF.Core 1.0.5 的接口默认值对齐）。

v1.0.4 (2026-07-09) — SECS/GEM 大帧拆包/粘包处理 + 并发安全 + 多处数据正确性修复

[新增]
- InternalClient 新增服务端下行数据帧缓冲区（ServerFrameBuffer，仿照 Worker 既有的
  LocationMessageBuffer 范式）：TCPClient 收到超过单次读取缓冲区的大消息时会被拆成多段
  分别触发 DataReceived，此前 InternalClient 直接假设每次事件即完整帧，长度校验失败即
  整段丢弃；后续分段的首字节若恰好是 0x02 还会被误判为连接状态变化事件。现统一先拼接
  再按帧提取，同时处理拆包与粘包，断线/重连时清空缓冲区防止残留半帧污染新连接。
- TCPClient/TcpServer 默认接收缓冲区从 8KB 统一提升为 64KB（新增
  PF.Core.Constants.CommunicationConstants.TcpReceiveBufferSize 共享常量），降低大消息
  拆包频率。
- TcpServer 新增每客户端发送锁：此前并发 SendAsync 同一连接会导致 WriteAsync 交错写入、
  破坏帧边界；配合 PF.SecsGem.Service.Worker 发往主机方向的数据统一改走单一发送通道
  串行化，消除此前 fire-and-forget 连发导致的字节流乱序风险。

[修复 — 并发/生命周期]
- TcpServer：ClientConnected/ClientDisconnected/DataReceived 三个事件此前传参不一致
  （Connected 传 GUID ClientId，Disconnected/DataReceived 却传 remote endpoint 字符串），
  导致按 ClientId 维护状态的订阅方（如 SecsGem 服务端每客户端缓冲区）收不到数据、断开时
  清不掉缓冲区、无法有效反馈客户端状态。三个事件统一改传 clientId。
- InternalClient.InitializationClient：事件订阅此前在连接成功（接收循环已启动）之后才
  完成，服务端在连接瞬间推送的 0x02 状态帧存在被漏订阅丢弃的竞态窗口，表现为偶发的
  "已连接但 SecsGemStatus 仍为 false、发送被拒绝"。现改为先订阅再连接。
- InternalClient.SendMessage/WaitForReplyAsync：此前发送请求后会把请求本体按 SystemBytes
  存入 ReplyMessageInfo，WaitForReplyAsync 轮询同一键会立刻取到请求自己当作"回复"返回，
  真实应答到达后被顶掉、永久滞留在字典中且不会被移除；现回复缓存只在收到真实应答时写入，
  并新增按时间留存自动清理（默认 2 分钟），避免 S6F11 等无人等待应答的事务造成字典
  无限增长。
- SecsGemCommandManger.ReloadAllCommandsAsync / IncentiveCommand.Reload /
  ResponseCommand.Reload：此前在持有非重入信号量的情况下调用了内部会再次获取同一信号量
  的方法，构成必然死锁（命令构建界面"重新加载"按钮点击即可复现）。现收窄信号量保护范围，
  消除嵌套获取。
- PF.SecsGem.Service.Worker：
  - 新主机连接到达时不再直接覆盖 SecsGemClientId 使旧连接变僵尸，改为先切换活跃连接
    再主动断开旧连接，且旧连接的断开事件不会误将新连接标记为离线。
  - 20 秒清空拼包缓冲区的判断此前是跨客户端共享的实例标志，且巨型帧传输超过 20 秒会被
    误判为脏数据清空；现下沉为每客户端独立的滞留计时。
  - _secsGemSystemParam 为空（数据库无系统配置）时此前直接空引用异常导致服务静默空转，
    现在服务启动阶段检测并记录明确错误后退出。

[修复 — 正确性]
- SecsGemMessageProcessor.ParseNumbers：单值路径此前会原地反转（就地修改）node.Data
  本体的字节序，导致同一条消息里"数组字段"与"单值字段"的 Data 字节序不一致；任何
  "收到消息后原样再转发/重序列化"的场景会使单值数值被静默改写。现统一先拷贝再转换，
  Data 字节序不再被解析过程破坏。
- SecsGemMessageUpdater（VID 变量更新）：此前所有数值类型直接
  `Data = BitConverter.GetBytes(value)` 写入主机序（小端），而序列化器按大端线格式原样
  输出，变量值上报后在对端读出错误数值（如 1 读成 16777216）。统一改为转大端后写入。
- SecsGemMessageTools：
  - CreateI4Node/CreateU2Node/CreateU4Node/CreateU8Node/CreateF4Node 同上问题，统一
    转大端。
  - 旧版 ConvertTobyteSecsNodeMessage 序列化器的长度字节数公式 `Ceiling(len/256)` 有误
    （len=256 算出错误的长度字节数，len&gt;768 直接抛异常），改为与新版序列化器一致的标准
    分级：≤255 用 1 字节、≤65535 用 2 字节、≤16777215 用 3 字节。
  - GenerateSystemBytes 此前用共享的非线程安全 Random 生成，多线程并发调用有内部状态
    损坏风险且随机值存在碰撞概率（导致应答错配）；改为原子递增计数器，保证唯一性。
  - FindCommands 此前用键的子串匹配（"S1F1" 会误命中 "S1F13"、"S11F1"），改为按
    Stream/Function 字段精确比较。
- ParamsManger.InitializationParams：返回值遗漏了配方配置加载结果（res3），配方加载
  失败时仍可能返回 true。
- IinternalClient.WaitForReplyAsync 接口默认超时声明为 5000ms，实现却是 10000ms，
  两者不一致；统一为 5000ms（对应 SECS 标准 T3）。

v1.0.3 (2026-07-07) — 机构报警自动桥接 + 通讯管理框架 + 硬件SDK扩展 + 多轮健壮性修复

[弃用]
- HKBarcodeScan（海康扫码枪 TCP 透传协议版）标记 [Obsolete]：新开发请改用
  MvCodeReaderBarcodeScan（海康官方 MvCodeReaderSDK.Net 托管封装，风险更低、支持图像采集）。
  仅标记警告（error: false），现网已部署的 "HKBarcodeScan" 硬件配置不受影响、继续可用；
  App.xaml.cs 中保留的工厂注册已显式抑制 CS0618 警告。

[删除]
- HKSdkBarcodeScan + HKRobotIdSdk（海康机器人 MvIdApi 原生 P/Invoke 封装）：全仓库确认零引用、
  未注册硬件工厂、从未实际可用，完整删除（含 PF.Modules.Parameter 侧的参数视图/映射器，
  见该包 changelog）。原文件自带注释承认结构体签名未经真实 SDK 头文件校对，属未验证代码，
  非"能用但暂不用"，删除无风险。

[新增 — StationBase&lt;T&gt;]
- WireMechanismAlarms()/MapMechanismAlarmCode 钩子：统一订阅机构报警并上抛为工站级报警，
  取代子类各自手写的订阅样板代码；子类构造函数末尾调用一次即可，幂等，Dispose 时自动对称解绑。

[新增 — 通讯管理]
- TcpServer/TCPClient/FileTransferChannel 实现 ICommunication，接入统一调试/管理体系；
  新增 SerialPortCommunication（基于 System.IO.Ports）；新增大文件传输通道（多 Lane 并行、
  CRC32+xxHash64 校验、断线重连）。

[新增 — 硬件 SDK]
- 集成基恩士（Keyence）智能相机 SDK 与海康（Hikvision）扫码枪 SDK。

[修复 — Keyence 相机]
- PR 响应校验条件写反（&amp;&amp; 应为 ||）修正；DetermineProgramExists 恢复原程式失败不再静默成功；
  InternalResetAsync 补上重连与程序号刷新；Trigger 失败改为抛异常；失败日志 Debug→Error；
  程序切换格式符最终定为 F4（对齐主控命令实际行为）。

[修复 — 并发与生命周期]
- TCPClient：发送/连接锁加超时、多处 await 补 ConfigureAwait(false)；
  IHardwareDevice/BaseDevice 实现 IAsyncDisposable；
  BaseAxisDevice 共享 Random 改用线程安全的 Random.Shared（原实现在多轴并发轮询下有状态损坏风险）；
  BaseMasterController.MaxDegreeOfParallelism 10→4（对齐文档）。

[清理]
- BaseMasterController 删除未使用/未赋值的死字段 _sync（IStationSyncService），
  各工站均已各自持有该服务实例，主控基类本身不消费。
- 系统性 Debug.WriteLine（带 [Conditional("DEBUG")]，Release 构建下被编译器整体裁掉）问题修复，
  TCPClient 等无 ILogService 可用的位置改用 System.Diagnostics.Trace.WriteLine。
- 相机 API 重命名：Tigger→Trigger、IPAdress→IPAddress、TiggerPort→TriggerPort 等
  （仅相机范围；工厂注册字符串 "KeyenceIntelligentCamera" 保持不变）。

v1.0.2 (2026-06-17) — SecsGem 初始化逻辑重构 + 指令加载容错

[行为变更 — SecsGem ParamsManger]
- InitializeAsync 加载顺序调整：
  旧逻辑：res1 &amp; res2 &amp; res3 全部成功后才执行命令管理器初始化；
          配方配置（res3）加载失败则直接返回 false，命令管理器不初始化。
  新逻辑：res1（系统配置）、res2（变量配置）、res3（配方配置）并行加载，
          命令管理器初始化（res4）始终执行，返回 res1 &amp; res2 &amp; res4；
          res3 失败不再阻塞初始化，SecsGem 服务空库时不再报错退出。

- LoadFormulaConfiguration 容错增强：
  IncentiveCommand / ResponseCommand 从数据库读取操作包进 try-catch；
  数据库为空或表不存在时静默返回空字典，不抛异常，不中断初始化流程。
  适用场景：首次部署、SecsGemConfig.db 尚未导入配方配置时。

v1.0.1 - 框架初始独立版本
		</PackageReleaseNotes>
		<Description>底层基础设施：BaseDevice、BaseMechanism、StationBase、TCP 通信基类</Description>
		<GeneratePackageOnBuild Condition="'$(Configuration)' == 'Release'">true</GeneratePackageOnBuild>
	</PropertyGroup>

</Project>
