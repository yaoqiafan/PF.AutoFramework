<Project Sdk="Microsoft.NET.Sdk">
	<Import Project="..\Common.Desktop.props" Condition="Exists('..\Common.Desktop.props')" />
	<ItemGroup>
		<PackageReference Include="Prism.Wpf" />
	</ItemGroup>
	<ItemGroup>
	  <ProjectReference Include="..\PF.Infrastructure\PF.Infrastructure.csproj" />
	  <ProjectReference Include="..\PF.UI.Infrastructure\PF.UI.Infrastructure.csproj" />
	</ItemGroup>

	<PropertyGroup>
		<IsModuleProject>true</IsModuleProject>
		<IsPackable>true</IsPackable>
		<Version>1.0.6</Version>
		<PackageReleaseNotes>
v1.0.6 (2026-07-21) — 硬件调试支持 [HardwareUI] 自定义设备路由

[新增]
- HardwareDebugViewModel.ExecuteNavigateToDebug 增加特性优先分支：设备类型标注 [HardwareUI("视图名")]
  时，按其 ViewName 导航到项目自定义调试视图（导航参数键统一为 "Device"），优先于内置类型的硬编码
  分发；未标注的内置设备（轴/IO/卡/相机/条码/光源）行为完全不变，向后兼容。
  依赖 PF.Core 1.0.8 的 HardwareUIAttribute。

v1.0.5 (2026-07-17) — Modbus 调试面板：报文预览 + 十六进制输入 + 原始报文直发 + 修复

[新增]
- 读写测试面板新增"报文预览"：按当前功能码/地址/值实时生成与实际发送帧逐字节一致的
  完整请求帧（RTU 含从站地址+CRC16，TCP 含 MBAP 头），通过
  IModbusMaster.BuildFrame + ModbusPduCodec 构建，参数无效时显示原因。TCP 固定事务号
  模式下，预览帧的事务号位会反映填写的固定值（递增模式仍占位 0000——发送时才分配，预览不消耗计数器）。
- 地址/写入值默认按十六进制填写，可勾选切换十进制：切换时已填的值在两种进制的字符串
  表示间自动换算（解析失败的文本原样保留）；从站地址与数量始终十进制，不受开关影响。
- 新增"原始报文直发"区域：直接发送十六进制 PDU 字符串（如 03 00 00 00 02，可含空格/
  短横线分隔），经 IModbusMaster.SendRawAsync 透传（不校验功能码回显，从站异常响应原样
  返回并在日志中标注），从站地址/CRC/MBAP 头由框架自动补全，并实时显示完整帧预览。
  用于测试框架未覆盖的功能码或自定义诊断报文。
- ModbusTcpDebugView 新增"事务号递增"勾选：递增模式下显示"重置事务号"按钮（计数器归零，
  下一笔请求从 1 开始）；不勾选（固定模式）下显示"事务号(HEX)"输入框，直接填写每次请求
  使用的事务号（0~FFFF，发送时生效；固定模式下预览帧事务号位反映填写的值，递增模式仍占位 0000）。
  便于与抓包比对、兼容固定事务号网关。
- ModbusRtuParamDialog 新增校验位/数据位/停止位三项（按 System.IO.Ports 枚举名解析，
  非法值回退 None/8/One），配合 Shell 工厂新增的 Parity/DataBits/StopBits 配置键，
  非默认帧格式（如 8E1）从站不再只能改代码。
- ModbusRtuParamDialog/ModbusTcpParamDialog 新增"响应超时(ms)/断线自动重连/重连间隔(ms)"
  三项，对应 Shell 工厂已支持的 TimeoutMs/AutoReconnect/ReconnectIntervalMs 配置键——
  此前这三个键只能手编配置文件，现可在参数面板直接调整（保存后重载全部通讯实例生效）。

[修复]
- ModbusRtuDebugViewModel/ModbusTcpDebugViewModel 的参数保存回调（async void）补
  try/catch：SaveConfigAsync/ReloadAllAsync 抛异常此前会直接崩掉进程，现兜住落到
  面板日志。
- 写多个线圈/寄存器的数量钳制到功能码上限（线圈 1968/寄存器 123）：此前误输大数
  会在 UI 线程逐个生成上万行输入项卡死界面。

v1.0.4 (2026-07-16) — Modbus RTU/TCP 主站调试视图

[新增]
- ModbusRtuDebugView/ModbusRtuDebugViewModel + ModbusTcpDebugView/ModbusTcpDebugViewModel：
  形态对齐既有 TcpClientDebugView（连接/打开状态、事件日志），另加一个按功能码测试
  读写的面板（覆盖全部 8 个功能码），随 CommunicationDebugViewModel 的 Category/Role
  树自动接入，无需改动该类。

v1.0.3 (2026-07-09) — 调试面板侧边栏图标调整

[变更]
- 通讯综合调试 Icon：DebugIcon → Lan
- 硬件综合调试 Icon：DebugIcon → RobotIndustrialOutline
- 设备模组调试 Icon：DebugIcon → CogOutline
- 流程工站调试 Icon：DebugIcon → SitemapOutline
  原 DebugIcon 被 4 个调试页共用（重复）且轮廓不适合描边展示，配合
  PF.Application.Base v1.0.3 新增的侧边栏悬停描边动画一并替换为各自独立、可完整描边的图标。

v1.0.2 (2026-07-07) — 通讯调试面板 + KeepAlive 适配

[新增]
- 通讯综合调试面板（Category/Role 两级分类树）；TCP 服务端/客户端/文件传输三个调试
  视图，含实时状态、测试收发、事件日志；均加"参数设置"入口（弹窗修改→存库→重新加载）。

[修复]
- TcpServerDebugViewModel/TcpClientDebugViewModel/FileTransferDebugViewModel 补充
  KeepAlive =&gt; true，配合 PF.UI.Infrastructure v1.0.2 的 RegionMemberLifetime 默认值
  变更，保留"同一 InstanceId 复用实例、LogEntries 等状态不清空"的既有语义。

v1.0.1 - 框架初始独立版本
		</PackageReleaseNotes>
		<Description>硬件调试面板：设备连接/断开、轴运动、IO 状态测试</Description>
		<GeneratePackageOnBuild Condition="'$(Configuration)' == 'Release'">true</GeneratePackageOnBuild>
	</PropertyGroup>
</Project>