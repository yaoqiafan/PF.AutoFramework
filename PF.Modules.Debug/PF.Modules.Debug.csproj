<Project Sdk="Microsoft.NET.Sdk">
	<Import Project="..\Common.Desktop.props" Condition="Exists('..\Common.Desktop.props')" />
	<ItemGroup>
		<PackageReference Include="Prism.Wpf" />
	</ItemGroup>
	<ItemGroup>
	  <ProjectReference Include="..\PF.Infrastructure\PF.Infrastructure.csproj" />
	  <ProjectReference Include="..\PF.UI.Infrastructure\PF.UI.Infrastructure.csproj" />
	</ItemGroup>

	<PropertyGroup>
		<IsModuleProject>true</IsModuleProject>
		<IsPackable>true</IsPackable>
		<Version>1.0.11</Version>
		<PackageReleaseNotes>
v1.0.11 (2026-08-28) — 界面操作日志接入：键目录 + 全量控件标记

[新增]
- OperationLogKeysDebug 键目录（[OperationLogKeyCatalog]，内层嵌套类名 = 页面名，const 字段 = Key，
  DescriptionAttribute = 默认描述），模块 OnInitialized 里 OperationLogKeyRegistry.
  RegisterAssembly(Assembly.GetExecutingAssembly()) 一行注册。
- 本模块页面根挂 OperationLog.PageName、可交互控件挂 OperationLog.Key：24 个页面
  226 个键，其中 120 个关键操作（硬件连接/断开、伺服使能、回零、急停、复位、模拟报警、绝对/相对/点动运动、点表增删改存、IO 翻转、相机采集启停/软触发/存图、GenICam 节点读写执行、全局与单设备仿真切换、工站全局启停/暂停/恢复/复位/停止、跨工站信号量操作、模组初始化/复位/停止、线扫启停、通讯连接/收发/清空、各参数对话框确认）默认启用记录；其余默认关闭，
  按项目在配置目录的 OperationLogConfig.xlsx 里勾选 Enabled 列即可打开，改过的值永远优先。
- 未标记/不支持的控件（密码框、Slider、DatePicker、页内导航选择器等）不产生任何日志。


v1.0.10 (2026-08-24) — 光源控制器调试页支持动态通道数（UI 结构调整）

[变更 — LightControllerDebugViewModel]
- 通道相关的四个固定属性（LightValue1~4）替换为 ObservableCollection&lt;LightChannelViewModel&gt;
  Channels，数量随设备 ILightController.ChannelCount（PF.Core 1.0.14）变化，每次导航到新设备
  都按其通道数重建；配套的待发值/脏标记数组同步改为动态长度数组。
- 新增 LightChannelViewModel：承载单通道的 Value，ValueChangedByUser 事件把用户改动
  （区别于读回赋值）转发给既有的防抖/下发逻辑；读回改走 ApplyReadValue 写入且不触发下发，
  取代此前基于单个 _suppressWrite 标志位的做法。

[变更 — LightControllerDebugView.xaml]
- 通道区域由写死的 4 行 Grid 改为 ItemsControl 绑定 Channels：每行是一个带刻度的 Slider
  （TickPlacement=BottomRight，仅作视觉刻度、未启用 IsSnapToTickEnabled）+ 一个可编辑 TextBox，
  二者绑定同一个 Value 属性、天然双向联动——滑块拖动与文本框直接输入均可精确控制亮度。

[依赖]
- 依赖 PF.Core 1.0.14 的 ILightController.ChannelCount 与 PF.Infrastructure 1.0.12 的对应实现。

v1.0.9 (2026-08-18) — 新增串口调试页

[新增]
- SerialPortDebugView + SerialPortDebugViewModel（注册键 SerialPortDebugView）：
  打开/关闭串口、原始字节收发、收发记录（最多 200 条，最新在前）。
  此前 5 个通讯调试页覆盖 TCP 服务端/客户端、文件传输、Modbus RTU/TCP，唯独没有裸串口。
- SerialPortParamDialog + ViewModel：串口名称 / 波特率 / 校验位 / 数据位 / 停止位，
  沿用其它通讯页"改参数 → 存库 → ReloadAllAsync → 重新绑定刷新后的实例"的流程。

[为什么需要它]
- 串口设备（如海康串口光源）出问题时，故障可能在链路本身（串口没开、线接错、波特率或校验位
  不匹配），也可能在设备协议层。设备调试页只能报"指令失败"，分不清是哪一层。
  这一页直接对着链路收发原始字节，先把物理链路择干净。
- Modbus RTU 页虽然也走串口，但它收发的是 Modbus 语义的报文，替代不了裸串口收发。

[实现要点]
- 收、发各有一个独立的十六进制开关：串口设备的协议既有 ASCII 文本型的（如海康光源的
  "SA0500#"），也有纯二进制型的。二进制报文按文本显示是一堆乱码和不可见字符没法核对，
  文本协议按十六进制看又很费劲，因此两个方向分开控制。
- 十六进制输入容忍空格/逗号/连字符/换行分隔与 0x 前缀；位数为奇数或含非法字符时
  【只提示不发送】——把半截报文丢给设备比不发更糟。
- 文本模式下把不可打印字符替换成 '.'，避免控制字符把日志行搅乱。
- 串口事件在 SerialPort 的接收线程上触发，统一转回 UI 线程再更新绑定属性。

[消费项目怎么让这一页有东西可点]
- 调试页只列出【已配置的】串口实例，装上包不会凭空多出串口节点。需要两步：
  ① App.xaml.cs 的 RegisterCommunicationFactories 里注册 "SerialPort" 工厂
    （配置键：必填 PortName，可选 BaudRate 缺省 9600、Parity/DataBits/StopBits 缺省 N81）；
  ② 默认参数或参数界面里加一条 Category=Serial、ImplementationClassName="SerialPort" 的通讯配置。
- 建议 AutoStart=false：串口的打开时机交给调试页或设备层驱动，避免通讯层与设备层抢着开口，
  也避免开发机上没有该串口时启动报错。
- 框架 Demo Shell 里有现成两条可参照：SerialPort_Example（裸串口，专供本页试手）
  与 HikLight_Serial（挂在海康光源设备下）。

v1.0.8 (2026-08-18) — 光源控制器调试页修复合集 + 亮度读回

[修复 — 关键]
- 状态轮询定时器从未启动：构造函数挂了 Tick、OnNavigatedFrom 里 Stop，却没有任何地方
  Start。结果连接状态与报警状态两盏灯一直停在初始值，页面显示与设备实际状态完全脱节。
  现在在 OnNavigatedTo 绑定设备后立即刷新一次并启动轮询（200ms）。
- 亮度下发是 fire-and-forget：四个 LightValueN 的 setter 直接调用 async 的
  SetLightValue 既不 await 也不接异常，下发失败在界面和日志里都没有任何痕迹
  （两个实现内部各自 catch，所以不崩，但也看不出没生效）。现统一经 RunAsync 包装，
  异常落 Hardware 分类日志，成功落 Debug 日志。
- COMAdress 属性会空引用：设备尚未注入（_lightController 为 null）时直接解引用；
  且该属性无变更通知，导航进来后界面不会刷新。现改为空安全 + 在 OnNavigatedTo 主动
  RaisePropertyChanged。串口由通讯层持有的实现（海康串口光源）ComName 为空，
  此时显示"—（串口由通讯配置持有）"而不是一片空白。

[新增 — 亮度下发防抖与串行化]
- 界面滑块是 UpdateSourceTrigger=PropertyChanged，拖一次会连发几十个 setter，而一次
  亮度下发是一笔串口事务（海康那款还要等应答，最长 3 秒），逐个下发会把串口打满、
  界面看着像卡死。现改为"记下最新值 → 停手 150ms 后只发最后一个值"，并用
  SemaphoreSlim(1,1) 保证同一时刻只有一笔在途；下发期间继续拖动会重新置脏触发下一轮，
  不会丢最后一次改动。
- 页面级 CancellationTokenSource 真正投入使用（此前声明后只 Cancel、从未传给任何调用）：
  离开调试页即取消在途的亮度下发，取消按正常路径吞掉不记错误日志。
  依赖 PF.Core 1.0.11 的 SetLightValue(token) 重载。

[新增 — 亮度读回]
- 「读取亮度」按钮：逐通道调用 ILightController.GetLightValue 读回设备实测值并刷新滑块。
  页面显示的不再只是"最近一次下发的值"。
- 进入页面且设备已连接时自动读一次，滑块初值不再固定从 0 开始；未连接则不自动读，
  避免四个通道连抛四次"未连接"刷屏。
- 读回是按钮触发、有意不做轮询：单通道读取是一笔串口问答（海康那款等应答最长 3 秒），
  四通道轮询一遍最坏十几秒；若按 200ms 轮询，串口会被读指令占满、下发根本挤不进去，
  且读回值会不停把用户正在拖的滑块拽回去。
- 读与写共用同一把信号量，链路上不会交叉占用；读回赋值时抑制属性 setter 的下发，
  避免"读到的值→setter→又发回设备"自激；单通道读取失败只记 Error 日志并继续读后续通道，
  某一路没接灯不会让整次读取作废。

[清理]
- 设备名默认文案"未选中扫码枪"改为"未选中光源控制器"（从扫码枪页复制粘贴留下的）。
- 设备存在但拿不到底层 BaseDevice 时给出明确文案，不再静默停留在初始文案。
- 页面图标由 📷 改为 💡（光源页此前用的是相机图标）。

v1.0.7 (2026-08-17) — 线扫三件套调试页（线阵相机 / 图像采集卡 / 线扫检测模组）+ 硬件树支持多类宿主

[新增 — 线阵相机调试页 LineScanCameraDebugView]
- 连接/断开/复位/模拟报警 + 枚举在线相机；面板状态一律【从设备读回】而非回显界面输入值——
  下发失败或被设备夹紧的值不会在界面上假装生效（RefreshParamsCommand 主动重读）。
- 配置下发（相机本体参数 + 行触发 + 编码器）、开流/停流、帧软触发、取一帧、
  按 BMP/PNG/TIFF 等格式存盘；预览改用 pf:ImageViewer（缩放/平移/适应窗口）。
- GenICam 属性树：一键枚举设备全部节点，按分类分组显示名称/类型/权限/当前值，
  并带常用节点的中文释义；支持单节点读取/写入/命令执行，无需先猜节点名。

[新增 — 图像采集卡调试页 FrameGrabberDebugView]
- 连接/断开/复位/模拟报警、刷新采集卡状态、枚举挂在本卡上的相机。
- 帧控制下发（帧长/帧超时/帧触发源/残帧策略等）与帧软触发；同样带 GenICam 属性树与
  单节点读写/命令执行。采集卡独立成页而非做成相机的附属面板——切帧发生在卡上，
  它本来就是一台独立设备。
- 帧控制的写入时机修正：必须在【打开相机之前】写入采集卡，否则相机开流时读到的是旧帧长。

[新增 — 线扫检测模组调试页 LineScanDetectionModuleDebugView]
- 随框架提供（模组本体在 PF.Infrastructure，无需每个项目自己写一份），
  导航 key 与 [MechanismUI] 的 LineScanDetectionModuleDebugView 一致。
- 初始化/复位/停止模组、移动到扫描起点、执行一次完整扫描、中止扫描；
  ScanProfile 各项经 pf:PropertyGrid 编辑，实时显示换算结果（帧长/行频/曝光上限/理论帧时间与
  帧超时）与 Validate() 的校验信息，校验不通过时"扫描"按钮直接禁用——配错在开扫前就被挡住；
  扫描结果用 pf:ImageViewer 预览。页面布局与其他模组调试页对齐。

[变更 — 硬件综合调试树]
- 树根不再等同于运动控制卡：图像采集卡同样是"下面挂子设备"的宿主，两类宿主一起构成第一层，
  采集卡节点显示为 [采集卡] 设备名。
- 子设备分组由 ParentCard 改为按 ParentDevice 分组（配合 PF.Core 1.0.10 的契约泛化）；
  "孤儿设备"判定由"不是 IMotionCard 且未被挂载"改为"不是任何宿主且未被挂载"，
  避免采集卡自己被当成孤儿重复出现。
- 点击路由新增 IFrameGrabberCard → FrameGrabberDebugView、ILineScanCamera →
  LineScanCameraDebugView 两个分支（均在 IIntelligentCamera 分支之前判定，
  导航参数键仍为 "Device"）。[HardwareUI] 特性优先的分支不变。

v1.0.6 (2026-07-21) — 硬件调试支持 [HardwareUI] 自定义设备路由

[新增]
- HardwareDebugViewModel.ExecuteNavigateToDebug 增加特性优先分支：设备类型标注 [HardwareUI("视图名")]
  时，按其 ViewName 导航到项目自定义调试视图（导航参数键统一为 "Device"），优先于内置类型的硬编码
  分发；未标注的内置设备（轴/IO/卡/相机/条码/光源）行为完全不变，向后兼容。
  依赖 PF.Core 1.0.8 的 HardwareUIAttribute。

v1.0.5 (2026-07-17) — Modbus 调试面板：报文预览 + 十六进制输入 + 原始报文直发 + 修复

[新增]
- 读写测试面板新增"报文预览"：按当前功能码/地址/值实时生成与实际发送帧逐字节一致的
  完整请求帧（RTU 含从站地址+CRC16，TCP 含 MBAP 头），通过
  IModbusMaster.BuildFrame + ModbusPduCodec 构建，参数无效时显示原因。TCP 固定事务号
  模式下，预览帧的事务号位会反映填写的固定值（递增模式仍占位 0000——发送时才分配，预览不消耗计数器）。
- 地址/写入值默认按十六进制填写，可勾选切换十进制：切换时已填的值在两种进制的字符串
  表示间自动换算（解析失败的文本原样保留）；从站地址与数量始终十进制，不受开关影响。
- 新增"原始报文直发"区域：直接发送十六进制 PDU 字符串（如 03 00 00 00 02，可含空格/
  短横线分隔），经 IModbusMaster.SendRawAsync 透传（不校验功能码回显，从站异常响应原样
  返回并在日志中标注），从站地址/CRC/MBAP 头由框架自动补全，并实时显示完整帧预览。
  用于测试框架未覆盖的功能码或自定义诊断报文。
- ModbusTcpDebugView 新增"事务号递增"勾选：递增模式下显示"重置事务号"按钮（计数器归零，
  下一笔请求从 1 开始）；不勾选（固定模式）下显示"事务号(HEX)"输入框，直接填写每次请求
  使用的事务号（0~FFFF，发送时生效；固定模式下预览帧事务号位反映填写的值，递增模式仍占位 0000）。
  便于与抓包比对、兼容固定事务号网关。
- ModbusRtuParamDialog 新增校验位/数据位/停止位三项（按 System.IO.Ports 枚举名解析，
  非法值回退 None/8/One），配合 Shell 工厂新增的 Parity/DataBits/StopBits 配置键，
  非默认帧格式（如 8E1）从站不再只能改代码。
- ModbusRtuParamDialog/ModbusTcpParamDialog 新增"响应超时(ms)/断线自动重连/重连间隔(ms)"
  三项，对应 Shell 工厂已支持的 TimeoutMs/AutoReconnect/ReconnectIntervalMs 配置键——
  此前这三个键只能手编配置文件，现可在参数面板直接调整（保存后重载全部通讯实例生效）。

[修复]
- ModbusRtuDebugViewModel/ModbusTcpDebugViewModel 的参数保存回调（async void）补
  try/catch：SaveConfigAsync/ReloadAllAsync 抛异常此前会直接崩掉进程，现兜住落到
  面板日志。
- 写多个线圈/寄存器的数量钳制到功能码上限（线圈 1968/寄存器 123）：此前误输大数
  会在 UI 线程逐个生成上万行输入项卡死界面。

v1.0.4 (2026-07-16) — Modbus RTU/TCP 主站调试视图

[新增]
- ModbusRtuDebugView/ModbusRtuDebugViewModel + ModbusTcpDebugView/ModbusTcpDebugViewModel：
  形态对齐既有 TcpClientDebugView（连接/打开状态、事件日志），另加一个按功能码测试
  读写的面板（覆盖全部 8 个功能码），随 CommunicationDebugViewModel 的 Category/Role
  树自动接入，无需改动该类。

v1.0.3 (2026-07-09) — 调试面板侧边栏图标调整

[变更]
- 通讯综合调试 Icon：DebugIcon → Lan
- 硬件综合调试 Icon：DebugIcon → RobotIndustrialOutline
- 设备模组调试 Icon：DebugIcon → CogOutline
- 流程工站调试 Icon：DebugIcon → SitemapOutline
  原 DebugIcon 被 4 个调试页共用（重复）且轮廓不适合描边展示，配合
  PF.Application.Base v1.0.3 新增的侧边栏悬停描边动画一并替换为各自独立、可完整描边的图标。

v1.0.2 (2026-07-07) — 通讯调试面板 + KeepAlive 适配

[新增]
- 通讯综合调试面板（Category/Role 两级分类树）；TCP 服务端/客户端/文件传输三个调试
  视图，含实时状态、测试收发、事件日志；均加"参数设置"入口（弹窗修改→存库→重新加载）。

[修复]
- TcpServerDebugViewModel/TcpClientDebugViewModel/FileTransferDebugViewModel 补充
  KeepAlive =&gt; true，配合 PF.UI.Infrastructure v1.0.2 的 RegionMemberLifetime 默认值
  变更，保留"同一 InstanceId 复用实例、LogEntries 等状态不清空"的既有语义。

v1.0.1 - 框架初始独立版本
		</PackageReleaseNotes>
		<Description>硬件调试面板：设备连接/断开、轴运动、IO 状态测试</Description>
		<GeneratePackageOnBuild Condition="'$(Configuration)' == 'Release'">true</GeneratePackageOnBuild>
	</PropertyGroup>
</Project>