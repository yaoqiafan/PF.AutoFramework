<UserControl
    x:Class="PF.Modules.Debug.Views.AuxEncoderDebugView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:oplog="clr-namespace:PF.UI.Infrastructure.Operation;assembly=PF.UI.Infrastructure"
    xmlns:opkeys="clr-namespace:PF.Modules.Debug"
    xmlns:pf="clr-namespace:PF.UI.Controls;assembly=PF.UI.Controls"
    xmlns:prism="http://prismlibrary.com/"
    prism:ViewModelLocator.AutoWireViewModel="True"
    oplog:OperationLog.PageName="AuxEncoderDebugView"
    d:DesignHeight="600"
    d:DesignWidth="900"
    mc:Ignorable="d">

    <pf:TransitioningContentControl TransitionMode="Right2LeftWithFade" TransitionStoryboard="{StaticResource Right2LeftWithFadeTransition}">
        <Grid Margin="15">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!--  设备头信息 + 操作按钮  -->
            <Border Grid.Row="0" Margin="0,0,0,15" Padding="15" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="6">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" Width="48" Height="48" Margin="0,0,15,0" CornerRadius="8">
                        <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="{DynamicResource TextIconBrush}" FontSize="{DynamicResource FontSize.H1.Large}" Text="🎚" />
                    </Border>

                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock FontSize="{DynamicResource FontSize.H2.Title}" FontWeight="Bold" Text="{Binding DeviceName}" />
                        <TextBlock Margin="0,4,0,0" FontSize="13" Text="{Binding DeviceDescription}" />
                        <TextBlock Margin="0,4,0,0" FontSize="12" Foreground="Gray">
                            <Run Text="通道号: " /><Run FontWeight="SemiBold" Text="{Binding Channel, Mode=OneWay}" />
                            <Run Text="   倍率: " /><Run FontWeight="SemiBold" Text="{Binding Multiplier, Mode=OneWay}" />
                        </TextBlock>
                    </StackPanel>

                    <StackPanel Grid.Column="2" Margin="0,0,20,0" VerticalAlignment="Center" Orientation="Horizontal">
                        <Button Height="32" Margin="0,0,10,0" Command="{Binding ConnectCommand}" oplog:OperationLog.Key="{x:Static opkeys:OperationLogKeysDebug+AuxEncoderDebugView.Connect}" Content="连接硬件" Style="{DynamicResource ButtonDefault}" />
                        <Button Height="32" Margin="0,0,10,0" Command="{Binding DisconnectCommand}" oplog:OperationLog.Key="{x:Static opkeys:OperationLogKeysDebug+AuxEncoderDebugView.Disconnect}" Content="断开连接" Style="{DynamicResource ButtonDefault}" />
                        <Button Height="32" Margin="0,0,10,0" Command="{Binding ResetCommand}" oplog:OperationLog.Key="{x:Static opkeys:OperationLogKeysDebug+AuxEncoderDebugView.Reset}" Content="模块复位" Style="{DynamicResource ButtonWarning}" />
                        <Button Height="32" Command="{Binding SimulateAlarmCommand}" oplog:OperationLog.Key="{x:Static opkeys:OperationLogKeysDebug+AuxEncoderDebugView.SimulateAlarm}" Content="模拟报警测试" Style="{DynamicResource ButtonDanger}" />
                    </StackPanel>

                    <StackPanel Grid.Column="3" VerticalAlignment="Center" Orientation="Horizontal">
                        <TextBlock Margin="0,0,5,0" VerticalAlignment="Center" Text="通讯状态:" />
                        <Border Padding="8,4" Background="#10B981" CornerRadius="4">
                            <TextBlock FontWeight="SemiBold" Foreground="White" Text="{Binding IsConnected, StringFormat='{}{0}'}" />
                        </Border>
                    </StackPanel>
                </Grid>
            </Border>

            <!--  位置监控 + 锁存测试  -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="15" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!--  当前位置 + 写入位置  -->
                <Border Grid.Column="0" Padding="15" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="6">
                    <StackPanel>
                        <TextBlock Margin="0,0,0,15" FontSize="{DynamicResource FontSize.H2.Title}" FontWeight="Bold" Text="编码器位置" />

                        <Border Padding="15,20" HorizontalAlignment="Stretch" Background="#F1F5F9" BorderBrush="#CBD5E1" BorderThickness="1" CornerRadius="6">
                            <StackPanel HorizontalAlignment="Center">
                                <TextBlock HorizontalAlignment="Center" FontSize="12" Foreground="Gray" Text="当前位置（轮询刷新）" />
                                <TextBlock Margin="0,6,0,0" HorizontalAlignment="Center" FontSize="28" FontWeight="Bold" Text="{Binding CurrentPosition}" />
                            </StackPanel>
                        </Border>

                        <TextBlock Margin="0,20,0,6" Text="写入 / 预置位置" />
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="10" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBox Grid.Column="0" Height="32" VerticalContentAlignment="Center" oplog:OperationLog.Key="{x:Static opkeys:OperationLogKeysDebug+AuxEncoderDebugView.TargetPosition}" Text="{Binding TargetPosition, UpdateSourceTrigger=PropertyChanged}" />
                            <Button Grid.Column="2" Height="32" Padding="15,0" Command="{Binding SetPositionCommand}" oplog:OperationLog.Key="{x:Static opkeys:OperationLogKeysDebug+AuxEncoderDebugView.SetPosition}" Content="写入" Style="{DynamicResource ButtonDefault}" />
                        </Grid>
                    </StackPanel>
                </Border>

                <!--  锁存测试  -->
                <Border Grid.Column="2" Padding="15" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="6">
                    <StackPanel>
                        <TextBlock Margin="0,0,0,15" FontSize="{DynamicResource FontSize.H2.Title}" FontWeight="Bold" Text="硬件锁存测试" />

                        <TextBlock Margin="0,0,0,6" Text="锁存器 ID" />
                        <TextBox Height="32" Margin="0,0,0,12" VerticalContentAlignment="Center" oplog:OperationLog.Key="{x:Static opkeys:OperationLogKeysDebug+AuxEncoderDebugView.LatchNo}" Text="{Binding LatchNo, UpdateSourceTrigger=PropertyChanged}" />

                        <Grid Margin="0,0,0,15">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="10" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Button Grid.Column="0" Height="32" Command="{Binding SetLatchModeCommand}" oplog:OperationLog.Key="{x:Static opkeys:OperationLogKeysDebug+AuxEncoderDebugView.SetLatchMode}" Content="设置锁存模式" Style="{DynamicResource ButtonDefault}" />
                            <Button Grid.Column="2" Height="32" Command="{Binding ReadLatchCommand}" oplog:OperationLog.Key="{x:Static opkeys:OperationLogKeysDebug+AuxEncoderDebugView.ReadLatch}" Content="读取锁存结果" Style="{DynamicResource ButtonDefault}" />
                        </Grid>

                        <Border Padding="15" Background="#F1F5F9" BorderBrush="#CBD5E1" BorderThickness="1" CornerRadius="6">
                            <StackPanel>
                                <Grid Margin="0,0,0,8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="锁存个数：" />
                                    <TextBlock Grid.Column="1" FontWeight="SemiBold" Text="{Binding LatchCount}" />
                                </Grid>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="锁存位置：" />
                                    <TextBlock Grid.Column="1" FontWeight="SemiBold" Text="{Binding LatchPosition}" />
                                </Grid>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>
    </pf:TransitioningContentControl>
</UserControl>
