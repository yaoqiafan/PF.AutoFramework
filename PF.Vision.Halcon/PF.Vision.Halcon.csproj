<Project Sdk="Microsoft.NET.Sdk">
  <Import Project="..\Common.props" Condition="Exists('..\Common.props')" />

  <PropertyGroup>
    <IsPackable>true</IsPackable>
    <Version>1.0.3</Version>
    <PackageReleaseNotes>
v1.0.3 (2026-08-27) — 修复 ShapeTemplateService.FindMatches 找模板必报 HALCON #1208

[修复 — 严重]
- FindMatches 里 NumLevels==0 时错误地把控制参数传成字符串 "auto"，抄的是
  CreateTemplate/CreateShapeModel 那边"NumLevels 默认值是 auto"的惯例。但
  find_shape_model 算子的 NumLevels 参数默认值本来就是整数 0（0 本身就是"自动计算金字塔层数"
  的哨兵值，算子文档没说它接受 "auto" 字符串），传字符串直接触发
  "HALCON error #1208: Wrong type of control parameter 8 in operator find_shape_model"，
  ROI 模板匹配（验证模板/联合试跑里调用 FindMatches 的地方）全部报错，无法找到任何模板。
  改为直接传 options.NumLevels（int，隐式转 HTuple），0/非 0 都不再做字符串转换。

v1.0.2 (2026-08-21) — 调试服务与 Debug 引擎解耦（含构造函数破坏性变更）

[破坏性变更 — HalconDebugService]
- 构造函数新增第三参 string procedureDirectory。走 AddVisionServices 注册的项目不受影响
  （扩展方法已同步传入，其对外签名未变）；手动 new HalconDebugService(...) 的地方必须补参数。

[修复 — 只读 API 不再凭空拉起 Debug 引擎]
- GetProcedureSignatureAsync / GetAvailableProcedures / LaunchHDevelop 原先都走
  GetOrCreate(EngineMode.Debug)，只为拿过程目录这一个字符串。代价是生产机上每次开机多出一个
  HDevEngine + 一条 LongRunning 专用系统线程 + 一个递归 FileSystemWatcher，永不使用。
  现在三者只用注入的目录，GetDebugEngine() 仅剩 EnableDebugServerAsync / ForceReloadAsync /
  RunTestAsync 三个真正需要引擎的调用点。
- 目录在构造函数中 Path.GetFullPath 规范化：LaunchHDevelop 会把它拼进 HDevelop.exe 的
  -external_proc_path 命令行，HALCON C 层不保证处理双斜杠/混合斜杠。

[修复 — 子目录里的过程取不到签名]
- 过程查找原先只看顶层，而过程枚举是 AllDirectories 递归的：子目录里的过程能出现在列表中，
  选中却解析不出参数。新增 Internal/HdevProcedureCatalog 统一两侧规则，查找优先级为
  顶层 .hdvp → 子目录 .hdvp → 顶层 .hdev → 子目录 .hdev（.hdvp 全局优先，与枚举去重规则一致）。
  过程名里的 * 和 ? 不再被当通配符解释；多处同名时按路径排序取首个，结果可复现。

[修复 — 未启动调试服务器时投作业会永久挂起]
- Debug 引擎配置为 WaitForDebugConnection=true + InfiniteTimeSpan。原先只要向 Debug 引擎投作业
  就会调 SetWaitForDebugConnection(true)，在过程入口等待一个永远不会到来的 HDevelop 连接，
  又没有超时兜底，Worker 线程永久卡死。现加 DebugServerStarted 门控，服务器未启动时正常执行。

[修复 — IsDebugServerActive 在引擎释放后失步]
- 状态原先缓存在 HalconDebugService 本地字段，ReleaseAsync(EngineMode.Debug) 释放引擎后仍报
  true，LaunchHDevelop 会去连一个已经不存在的调试服务器。改为以引擎上的 DebugServerStarted
  为唯一真相，ActivePort / Password 一并跟随回落。
- DisableDebugServerAsync 的守卫同步改写：引擎已释放时直接返回，不再为了停一个不存在的服务器
  而把引擎重新拉起来。

依赖：需 PF.Core 1.0.13+。

v1.0.1 (2026-07-07) — 视觉引擎核心健壮性修复（7 项）

[修复 — 严重]
- Worker 线程防崩溃：过程文件缺失时 LoadProcedureFromFile 抛 FileNotFoundException 曾
  杀死 Worker 循环，导致此后所有视觉调用永久挂起；DoLoad/DoExecute 现捕获全部异常转为
  失败结果，Worker 循环外再加最后防线。
- 真实超时/取消：原超时只覆盖入队、await tcs.Task 无限等待；SubmitAndAwaitAsync 现覆盖
  排队+执行全程，超时返回失败结果，主动取消抛 OperationCanceledException。
- HObject 泄漏：管线黑板改为持有独立句柄副本（CopyObj）并随管线结束统一释放；所有权契约
  已写入 IVisionService/IVisionResult 文档（返回值归调用方所有，事件参数仅回调期间有效）。

[修复 — 中]
- 管线条件解析失败从"视为 true 继续"改为终止管线；HTupleToValue 按 HTuple.Type 分派；
  FileSystemWatcher 加防抖；VisionContextManager 去掉与释放竞态的无锁快速路径。

v1.0.0 - Halcon HDevEngine 视觉服务层初始版本：过程执行引擎、ROI 管理、视觉工作流管线、DI 扩展方法。
    </PackageReleaseNotes>
    <GenerateDocumentationFile>false</GenerateDocumentationFile>
    <Description>Halcon HDevEngine 视觉服务层（Layer 04）：过程执行引擎、ROI 管理、视觉工作流管线；零 Prism.Wpf / WPF 依赖，可独立于 UI 引用。</Description>
    <GeneratePackageOnBuild Condition="'$(Configuration)' == 'Release'">true</GeneratePackageOnBuild>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Prism.Core" />
  </ItemGroup>

  <!-- 通配引用 DLL\HALCON\ 目录下所有托管 DLL -->
  <!-- 注意：必须用 Target 内批处理，直接在 ItemGroup 中用 transform 会丢失路径元数据 -->
  <ItemGroup>
    <_HalconDll Include="$(HalconDllDir)*.dll" Exclude="$(HalconDllDir)*xl.dll" />
  </ItemGroup>

  <Target Name="AddHalconReferences_Vision" BeforeTargets="ResolveAssemblyReferences">
    <ItemGroup>
      <Reference Include="%(_HalconDll.Filename)">
        <HintPath>%(_HalconDll.FullPath)</HintPath>
        <Private>true</Private>
      </Reference>
    </ItemGroup>
  </Target>

  <ItemGroup>
    <ProjectReference Include="..\PF.Core\PF.Core.csproj" />
  </ItemGroup>

</Project>
