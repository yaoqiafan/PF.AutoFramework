<Project Sdk="Microsoft.NET.Sdk">
		<Import Project="..\Common.Desktop.props" Condition="Exists('..\Common.Desktop.props')" />
		<ItemGroup>
			<PackageReference Include="Prism.Wpf" />
			<PackageReference Include="NPOI" />
		</ItemGroup>
		<ItemGroup>
			<ProjectReference Include="..\PF.UI.Infrastructure\PF.UI.Infrastructure.csproj" />
			<ProjectReference Include="..\PF.Infrastructure\PF.Infrastructure.csproj" />
		</ItemGroup>

		<PropertyGroup>
			<IsModuleProject>true</IsModuleProject>
			<IsPackable>true</IsPackable>
			<Version>1.0.5</Version>
			<PackageReleaseNotes>
v1.0.5 (2026-09-18) — 用户管理新增批量导入 / 批量删除 / 列表视图

[新增 — 批量导入]
- UserManagementView 工具栏新增"导出模板""批量导入"两个按钮。导出模板产出一份仅含
  「工号/密码」两列的 xlsx；导入时逐行校验（空值、文件内工号重复判失败；工号与系统已有
  账号——含内置 Operator/Engineer/Administrator/SuperUser——重名判跳过、不覆盖），弹窗展示
  成功/跳过/失败明细，用户确认后才批量落盘。导入账号权限统一固定为 Operator。
- 批量导入仅管理员（Administrator）及以上可用，非管理员按钮置灰（IsEnabled 绑定，非隐藏），
  命令内部同时做一次防御性二次校验。
- 新增 UserImportExcelHelper（NPOI 读写 xlsx）；本包新增 NPOI 包引用（版本由
  Directory.Packages.props 中央管理）。

[新增 — 批量删除]
- 卡片视图（CoverView）与新增的列表视图（DataGrid）顶部均新增"全选 / 已选择 N 项 / 批量
  删除"工具条，两个视图共用同一份勾选状态，切换视图不丢失已勾选项。
- 权限门槛按等级逐行判定，不是全局开关：只能勾选/删除权限等级严格低于当前登录账号的用户，
  同级或更高级（含账号自己）一律不可勾选——用等级比较天然排除了"删自己"，不需要额外特判。
- 二次确认弹窗列出待删用户名，确认后批量调用既有 DeleteUserAsync，复用原有落盘与日志路径。

[新增 — 列表视图]
- 标题"用户管理"右侧新增 pf:ButtonGroup + 两个 RadioButton（🗂 卡片视图 / 📋 列表视图）双向
  绑定切换；列表视图为只读 DataGrid（用户名/权限等级/可访问页面/删除），编辑仍走卡片视图。

[修复 — 联动修复 PF.UI.Infrastructure 的 WaitDialogViewModel]
- 批量导入/删除包一层"请稍候"等待框（ExecuteWithWaitAsync）时，若被包裹的任务跑得足够快
  （如只写一两条参数），会在弹窗真正显示完成前触发关闭，抛出
  "DialogCloseListener 未初始化"未处理异常。本包是第一个用快速任务触发该竞态的调用方，
  根因修复见 PF.UI.Infrastructure 1.0.6 changelog。

v1.0.4 (2026-09-15) — 修复 RuntimeIdentifier 误设导致的处理器架构标记污染

[修复]
- 框架仓库共享的 Common.Desktop.props 曾错误设置 RuntimeIdentifier=win-x64（该属性只应用于
  真正消费 PF.Vendor.Infrastructure 的项目，本仓库自身没有项目这样做），导致 2026-09-15 早间
  批量打包时本包连同其余 10 个 net8.0-windows 库一起被打上 win-x64 专属架构标记，AnyCPU
  消费项目引用时报"处理器架构不匹配"警告。本次仅移除该属性重新打包，代码无变化。

v1.0.3 (2026-08-28) — 界面操作日志接入：键目录 + 全量控件标记

[新增]
- OperationLogKeysIdentity 键目录（[OperationLogKeyCatalog]，内层嵌套类名 = 页面名，const 字段 = Key，
  DescriptionAttribute = 默认描述），模块 OnInitialized 里 OperationLogKeyRegistry.
  RegisterAssembly(Assembly.GetExecutingAssembly()) 一行注册。
- 本模块页面根挂 OperationLog.PageName、可交互控件挂 OperationLog.Key：3 个页面
  15 个键，其中 8 个关键操作（登录、登出、页面授权、应用默认权限、保存权限、新增/保存/删除用户）默认启用记录；其余默认关闭，
  按项目在配置目录的 OperationLogConfig.xlsx 里勾选 Enabled 列即可打开，改过的值永远优先。
- 未标记/不支持的控件（密码框、Slider、DatePicker、页内导航选择器等）不产生任何日志。


v1.0.2 (2026-07-09) — 权限模块侧边栏图标调整

[变更]
- 权限获取（登录）Icon：LoginInIcon → Login
- 窗体权限更改 Icon：SettingIcon → KeyVariant
- 用户管理 Icon：NailGeometry → AccountCogOutline
  配合 PF.Application.Base v1.0.3 新增的侧边栏悬停描边动画，原图标存在描边观感差或
  语义不符的问题，一并替换。

v1.0.1 - 框架初始独立版本
			</PackageReleaseNotes>
			<Description>身份认证模块：登录、角色管理、细粒度权限 UI</Description>
			<GeneratePackageOnBuild Condition="'$(Configuration)' == 'Release'">true</GeneratePackageOnBuild>
		</PropertyGroup>
	</Project>