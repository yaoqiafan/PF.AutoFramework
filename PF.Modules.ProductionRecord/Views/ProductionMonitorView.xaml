<UserControl
    x:Class="PF.Modules.Production.Views.ProductionMonitorView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:pf="clr-namespace:PF.UI.Controls;assembly=PF.UI.Controls"
    xmlns:prism="http://prismlibrary.com/"
    prism:ViewModelLocator.AutoWireViewModel="True">
    <pf:TransitioningContentControl TransitionMode="Right2LeftWithFade" TransitionStoryboard="{StaticResource Right2LeftWithFadeTransition}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!--  标题栏  -->
            <Border
                Grid.Row="0"
                Padding="12,8"
                Background="{DynamicResource ThirdlyRegionBrush}">
                <TextBlock
                    VerticalAlignment="Center"
                    FontSize="16"
                    FontWeight="Bold"
                    Foreground="{DynamicResource PrimaryTextBrush}"
                    Text="生产数据实时监控" />
            </Border>

            <!--  过滤条  -->
            <Border
                Grid.Row="1"
                Padding="10,6"
                Background="{DynamicResource BorderBrush}">
                <StackPanel VerticalAlignment="Center" Orientation="Horizontal">
                    <TextBlock
                        Margin="0,0,4,0"
                        VerticalAlignment="Center"
                        Foreground="{DynamicResource SecondaryTextBrush}"
                        Text="类型：" />
                    <TextBox
                        Width="120"
                        Margin="0,0,16,0"
                        Padding="4,2"
                        Background="{DynamicResource SecondaryRegionBrush}"
                        BorderBrush="{DynamicResource BorderBrush}"
                        Foreground="{DynamicResource PrimaryTextBrush}"
                        Text="{Binding FilterRecordType, UpdateSourceTrigger=PropertyChanged}" />

                    <Button
                        Margin="0,0,8,0"
                        Padding="12,4"
                        Style="{DynamicResource ButtonDefault}"
                        Command="{Binding ClearCommand}"
                        Content="清空" />

                    <Button
                        Margin="0,0,8,0"
                        Padding="12,4"
                        Style="{DynamicResource ButtonSuccess}"
                        Command="{Binding ExportCommand}"
                        CommandParameter="excel"
                        Content="导出 Excel" />

                    <Button
                        Padding="12,4"
                        Style="{DynamicResource ButtonDefault}"
                        Command="{Binding ExportCommand}"
                        CommandParameter="csv"
                        Content="导出 CSV" />
                </StackPanel>
            </Border>

            <!--  数据列表  -->
            <ListView
                Grid.Row="2"
                Background="{DynamicResource SecondaryRegionBrush}"
                BorderThickness="0"
                Foreground="{DynamicResource SecondaryTextBrush}"
                ItemsSource="{Binding RecentRecords}"
                SelectionMode="Single">
                <ListView.View>
                    <GridView>
                        <GridViewColumn
                            Width="155"
                            DisplayMemberBinding="{Binding RecordTime, StringFormat={}{0:yyyy-MM-dd HH:mm:ss}}"
                            Header="采集时间" />
                        <GridViewColumn
                            Width="100"
                            DisplayMemberBinding="{Binding RecordType}"
                            Header="类型" />
                        <GridViewColumn
                            Width="200"
                            DisplayMemberBinding="{Binding TypeFullName}"
                            Header="数据类型" />
                        <GridViewColumn
                            Width="400"
                            DisplayMemberBinding="{Binding JsonValue}"
                            Header="JSON 数据" />
                    </GridView>
                </ListView.View>
            </ListView>

            <!--  状态栏  -->
            <Border
                Grid.Row="3"
                Padding="10,4"
                Background="{DynamicResource ThirdlyRegionBrush}">
                <TextBlock Foreground="{DynamicResource ThirdlyTextBrush}">
                    <Run Text="共 " />
                    <Run
                        FontWeight="Bold"
                        Foreground="{DynamicResource PrimaryTextBrush}"
                        Text="{Binding TotalCount}" />
                    <Run Text=" 条（最多保留 500 条）" />
                </TextBlock>
            </Border>
        </Grid>
    </pf:TransitioningContentControl>
</UserControl>
