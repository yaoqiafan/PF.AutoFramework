<UserControl
    x:Class="PF.WorkStation.AutoOcr.UI.UserControls.ChangeLotView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:behaviors="clr-namespace:PF.UI.Infrastructure.Behaviors;assembly=PF.UI.Infrastructure" 
    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:PF.WorkStation.AutoOcr.UI.UserControls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="200"
    d:DesignWidth="300"
    mc:Ignorable="d">

    <UserControl.Resources>
        <Style BasedOn="{StaticResource TextBlockBaseStyle}" TargetType="TextBlock">
            <Setter Property="FontSize" Value="{DynamicResource FontSize.Body.Standard}" />
        </Style>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height=" *" />
            <RowDefinition Height=" *" />
            <RowDefinition Height=" 50" />
        </Grid.RowDefinitions>
        <Grid Grid.Row=" 0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width=" auto" />
                <ColumnDefinition Width=" *" />
            </Grid.ColumnDefinitions>
            <TextBlock Margin="10,0,0,0" Text="内部批次:" />
            <TextBox
                Grid.Column=" 1"
                Margin="10,10,10,10"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch"
                Text="{Binding LotId, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" behaviors:EnterKeyTraversalBehavior.IsEnabled="True">
                
            </TextBox>
        </Grid>

        <Grid Grid.Row=" 1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width=" auto" />
                <ColumnDefinition Width=" *" />
            </Grid.ColumnDefinitions>
            <TextBlock Margin="10,0,0,0" Text="工       号:" />
            <TextBox 
                Grid.Column=" 1"
                Margin="10,10,10,10"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch"
                Text="{Binding UserId, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"   >
                <TextBox.InputBindings>
                    <KeyBinding Command="{Binding ConfirmCommand}"  Key="Enter" />
                </TextBox.InputBindings>
            </TextBox>
            
        </Grid>


        <Grid Grid.Row=" 2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width=" *" />
                <ColumnDefinition Width=" *" />
            </Grid.ColumnDefinitions>
            <Button
                Height=" 40"
                Margin="20,5,5,5"
                Command="{Binding ConfirmCommand}"  
                Content="确定" />
            <Button
                Grid.Column=" 1"
                Height=" 40"
                Margin="5,5,20,5"
                Command="{Binding CancelCommand}"
                Content="取消" />
        </Grid>
    </Grid>
</UserControl>
