<Project Sdk="Microsoft.NET.Sdk">

	<Import Project="..\Common.props" Condition="Exists('..\Common.props')" />
	<ItemGroup>
		<PackageReference Include="log4net" />
		<PackageReference Include="Microsoft.EntityFrameworkCore" />
		<PackageReference Include="Microsoft.EntityFrameworkCore.Sqlite" />
		<PackageReference Include="NPOI" />
		<PackageReference Include="Prism.Core" />
	</ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\PF.Data\PF.Data.csproj" />
    <ProjectReference Include="..\PF.Infrastructure\PF.Infrastructure.csproj" />
  </ItemGroup>

	<PropertyGroup>
		<IsPackable>true</IsPackable>
		<Version>1.0.3</Version>
		<PackageReleaseNotes>
v1.0.3 (2026-07-07) — 通讯管理服务 + 日志/硬件服务健壮性修复

[新增]
- CommunicationManagerService：结构对齐 HardwareManagerService，支持 AutoStart 开关，
  统一管理 TCP/串口/文件传输等通讯实例的注册、启停与生命周期。

[修复]
- LogService.ProcessLogQueueAsync 加 while(!_disposed) 重启壳 + 单条日志 try/catch，
  订阅者异常不再永久杀死日志消费任务（对齐 AlarmService 既有范本）；
  生产者-消费者队列由 BlockingCollection 迁移至 Channel&lt;LogEntry&gt;。
- HardwareManagerService._configLock：锁内仅同步操作、await 移到锁外，避免长时间持锁；
  GetAllConfigs 返回防御性副本。
- ParamService 仓储工厂缓存编译表达式，消除高频反射开销。
- Alarm/Log/Production 三服务实现 IAsyncDisposable。
- 系统性 Debug.WriteLine（Release 下被编译器裁掉）问题修复：HardwareManagerService/
  UserService 改用已有的 _logger/_systemLogger，其余无 logger 可用处改用 Trace.WriteLine。

v1.0.2 (2026-06-17) — 报警服务全链路 SECS/GEM 字段透传 + LogService 初始化 Bug 修复

[AlarmService — 新功能]
- TriggerAlarm：构造 AlarmRecord 时补填 MessageEn / MessageID / MessageIDHex，
  触发的报警记录完整携带 SECS/GEM 上传所需元数据，无需上传侧二次查字典。
- GetHistoryAlarms：历史记录从数据库映射时同步补填三个新字段，
  历史查询结果与实时报警记录字段保持一致。

[AlarmDictionaryService — 新功能]
- 反射扫描路径（代码内置报警）：从 [AlarmInfo] 特性读取 MessageEn / MessageID /
  MessageIDHex 并写入 AlarmInfo 模型。
- 数据库加载路径（AlarmDefinitions 扩展报警）：同步从 AlarmDefinitionEntity
  映射 MessageEn / MessageID / MessageIDHex。
- 兜底路径（未知报警码）：返回默认值 MessageID=-1 / MessageIDHex="NoneError"，
  保证上传侧收到的字段始终非 null。

[LogService — Bug 修复]
- 修复初始化顺序错误：_log4netLogger 赋值移至 InitializeLog4Net() 调用之后，
  修复旧版中因 log4net 未完成初始化即赋值导致 logger 可能为 null 的问题，
  表现为启动阶段前几条日志静默丢失。

v1.0.1 - 框架初始独立版本
		</PackageReleaseNotes>
		<Description>核心业务服务：ParamService、HardwareManagerService、ProductionDataService、LogService</Description>
		<GeneratePackageOnBuild Condition="'$(Configuration)' == 'Release'">true</GeneratePackageOnBuild>
	</PropertyGroup>

</Project>
