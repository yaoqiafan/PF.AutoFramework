<Project Sdk="Microsoft.NET.Sdk">
	<Import Project="..\Common.Desktop.props" Condition="Exists('..\Common.Desktop.props')" />

	<PropertyGroup>
		<IsPackable>true</IsPackable>
		<Version>1.0.7</Version>
		<PackageReleaseNotes>
v1.0.7 (2026-08-28) — 操作日志总开关 + 框架自身页面接入

[新增]
- CommonSettings 实现 IOperationLogSettings：新增 EnableOperationLog 总开关（参数界面
  "A.系统参数" 分类第 8 项，PropertyGrid 可见，默认 true）。与项目级 Excel 的按 Key 开关
  是两层粒度——总开关关闭时整个功能不生效，优先级更高。
- PFApplicationBase：RegisterTypes 注册 IOperationLogSettings 单例；OnInitialized 里自动
  OperationLogKeyRegistry.RegisterAssembly(框架目录) + OperationLogInterceptor.Attach()，
  消费项目继承即得，无需任何接入代码。
- OperationLogKeysBase 框架键目录：MainWindow（切换主题、侧边栏导航切换——关键）、
  MainView（打开说明书/检查更新/联系技术支持/关于软件）、CommonParamView（保存公共参数——关键）。
- MainWindow 的 pf:SideMenu 挂 Key 记录导航轨迹；MainView 四个常用操作按钮、
  CommonParamView 保存按钮同步标记。


v1.0.6 (2026-08-18) — 新增 UsesSecsGemService 开关：不用 SECS/GEM 的项目可整体关闭

[新增]
- PFApplicationBase.UsesSecsGemService（protected virtual，默认 true）。
  默认值保持既有行为，既有项目升级后无需任何改动。
  不需要 SECS/GEM 的项目在自己的 App 中写一行 override 即可整体关闭：
      protected override bool UsesSecsGemService => false;

[关闭后跳过的三处]
- RegisterSecsGemServices 整段 DI 注册（ISecsGemManager / ICommandManager / IinternalClient 等），
  同时不再在项目配置目录下创建空的 SecsGemConfig.db。
  门开在调用点而非方法体内：子类即便整体重写了 RegisterSecsGemServices，也保证一行都不执行。
- VerifySecsServiceProjectBinding 服务项目绑定校验。
- 模块目录中命名空间以 "PF.Modules.SecsGem." 开头的模块。即便子类的 ConfigureModuleCatalog
  里仍写着 AddModule&lt;SecsGemModule&gt;()，也会被拦下不加载——SECS/GEM 模块启动时要解析
  ISecsGemManager，而开关关闭后这些服务根本没注册，模块一旦加载就是崩在启动阶段。
  与其要求每个项目记得同时改两处，不如在目录层兜住。

[消费项目还要自己做的事 — 重要]
- 本开关只管框架自己那部分，管不到项目在 RegisterMechanismsAndStations / RegisterTypes 里
  写的注册。项目若注册了依赖 ISecsGemManager 的机构，关掉开关后启动会崩在解析 MainWindow 上：
  MainWindow 构造注入的是 IEnumerable&lt;IMechanism&gt;，DryIoc 会把【每一个】已注册机构都构造出来，
  其中一个依赖解析不到，整个 MainWindow 就解析失败（DryIoc 报 UnableToResolveUnknownService）。
  这类注册要用 if (UsesSecsGemService) 包起来。
- 构造注入了 ISecsGemManager 的 ViewModel 不会在启动时崩，而是导航到该页时才崩。
  改为注入 IContainerProvider、先 IsRegistered&lt;ISecsGemManager&gt;() 判断再 Resolve 即可
  （与 MainWindowViewModelBase 的既有做法一致）。
- 框架 Demo Shell 的 App.xaml.cs 里三处都有现成示例：机构注册、机构初始化名单、模块目录。

[修复了什么实际问题]
- 服务项目绑定校验此前是无条件执行的，唯一的跳过条件是"服务 exe 不存在"。但它解析的是
  SCM 中【实际注册】的 ImagePath，而该服务全机唯一、不按项目区分——一机多项目时，
  只要有任何一个项目装过该服务，这个判断就不成立。
- 后果：一个压根不用 SECS/GEM 的项目，在装过该服务的机器上启动会弹模态告警，
  说"当前注册的服务属于项目 X，而本程序是 Y"，并建议"重新运行本项目的安装包以接管服务"。
  而真按提示做，就把项目 X 正在用的服务抢走了。
- 现在该校验只在 UsesSecsGemService=true 时执行。

[备注]
- MainWindowViewModelBase 早已用 IsRegistered&lt;ISecsGemManager&gt;() 判断后再解析，
  开关关闭时 SecsGemManager 属性为 null，基类自身不再有其它引用点，无需额外处理。

v1.0.5 (2026-08-03) — 配置路径按项目隔离（破坏性变更，升级前必须执行现场迁移）

[破坏性变更 — 升级前必读]
配置目录由 D:\PFConfig\PFAutoFrameWork\ 改为 D:\PFConfig\PFAutoFrameWork\{项目名}\。
已部署机台【必须】先执行 Installer\Migrate-Config.ps1 把旧布局搬进项目子目录，
否则升级后参数、硬件配置、通讯配置、配方、点表全部回到默认值，SecsGem 服务拒绝启动。
详见 Installer\现场升级迁移说明.md。

[新增]
- PFApplicationBase.ProjectName 虚属性：默认取入口程序集名，作为配置隔离子目录名。
  新增实例构造函数，在其中调用 ConstGlobalParam.Initialize(ProjectName)——必须放构造函数
  而非 OnStartup/CreateShell/RegisterTypes：C# 中派生类字段初始化器先于基类构造函数体执行，
  子类以 => "XXX" 形式重写是安全的；其余入口都晚于 App 实例构造，期间已可能有类型触碰
  ConfigPath 并抛 TypeInitializationException。
  重写该属性的项目必须同步设置 installer.conf 的 PROJECT_NAME。
- PFApplicationBase.VerifySecsServiceProjectBinding 虚方法：Splash 阶段校验当前注册的
  SECS/GEM 服务是否属于本项目，不一致时写日志并弹窗告警（只告警不阻断——产线上因配置
  不一致导致软件起不来，代价比让人看见告警更大）。未部署该服务的项目自动跳过。
  校验放主程序而非服务：服务运行在 Session 0 弹不出对话框，且它没有参照物、只能发现
  "没配 ProjectName"，发现不了"配的是别的项目"。

[背景]
AppParamDbContext.EnsureDefaultParametersCreatedAsync 会删除"不在本项目默认参数集里"的
参数行。同机换项目运行时，A 的参数在 B 眼里全是废弃行，四张表被整批清空。

v1.0.4 (2026-07-27) — 状态栏设备项支持多态（三态及以上）显示

[新增]
- DeviceStatusItem 新增 StatusText / StatusBrush / UseCustomStatus 三个成员：设置 StatusText
  后状态栏该项自动从 IsConnected 二值灯切换为「自定义文本 + 颜色」的多态显示，用于承载三态
  及以上的枚举状态（如 SECS 控制状态：离线/本地/远程）。枚举→文本/颜色的映射由消费方
  （外接转换器）负责，框架只承载结果、不感知具体业务枚举，保持通用。
- MainWindow 状态栏 ItemTemplate 相应改造：UseCustomStatus 为真时灯用 StatusBrush、文本用
  StatusText，否则回退原 IsConnected（已连接/未连接）二值灯。现有仅用 Label/IsConnected 的
  设备项行为完全不变，向后兼容。

v1.0.3 (2026-07-09) — 主显界面下沉框架 + 侧边栏图标描边动效 + 版本徽章

[新增]
- MainWindowViewModelBase 新增 ProductImagePath/ProductDescription/UserManualPath 三个
  虚属性；框架新增 MainView(Model) 作为各消费项目共用的主显界面（左侧产品图片+简介左右
  布局卡片，简介以自定义样式 ContentControl 呈现并带加载动效；右侧品牌信息+常用操作区，
  含"打开说明书"真实功能与三个占位操作），随 PFApplicationBase.RegisterTypes 统一注册。
  消费项目只需在 Shell 层 MainWindowViewModel 子类中重写以上三属性即可接入，不必再各自
  维护一份 MainView（PF.WorkStation.AutoOcr.UI 已同步删除原 MainView 三文件作为示例）。
- MainWindowViewModelBase 绑定标题栏版本徽章（SoftWareVersion/RuntimeVersion）。

[改进]
- 侧边栏 MainWindow 子菜单图标：鼠标进入时轻微放大并播放一次路径描边动画（先隐藏填充→
  精确计算长度匀速描边→填充淡入复原），离开后复位可重复触发；新增 ComputeStrokePathLength
  替代控件库自带对多段路径估算严重失真的长度算法，避免不同图标描边速度不一致。
- CommonParamView 图标由 AudioGeometry 改为 ViewListOutline（原图标语义不符且描边观感差）。

[子包版本对照]
本次侧边栏图标一并调整的消费方需同步升级：PF.Modules.Alarm 1.0.2→1.0.3、
PF.Modules.Debug 1.0.2→1.0.3、PF.Modules.Halcon 1.0.1→1.0.2、
PF.Modules.Identity 1.0.1→1.0.2、PF.Modules.Logging 1.0.1→1.0.2、
PF.Modules.Parameter 1.0.2→1.0.3、PF.Modules.ProductionRecord 1.0.1→1.0.2、
PF.Modules.SecsGem 1.0.3→1.0.4。

v1.0.2 (2026-07-07) — 修复老库升级启动崩溃（CommunicationParams 建表 SQL 缺 Remarks 列）

[修复 — 关键，v1.0.1 升级必装]
- v1.0.1 为老库补建 HardwareParams/CommunicationParams 的裸 SQL 漏掉了 Remarks 列
  （EF 模型经 BasicEntity 映射该列），导致所有已部署机台（SystemParamsCollection.db
  已存在、CommunicationParams 表由裸 SQL 补建）启动时参数种子阶段首次查询即抛
  "no such column: Remarks" 崩溃。全新数据库由 EnsureCreatedAsync 按完整模型建表
  不受影响——这正是开发机测不出的原因。
- 两处建表 SQL 补上 Remarks 列；并新增幂等补列逻辑（PRAGMA 检查后 ALTER TABLE），
  已被 v1.0.1 建出缺列表的机器升级后自动修复，无需人工干预。

v1.0.1 (2026-07-07) — 通讯管理集成 + 关闭流程健壮性修复

[新增]
- 通讯实例数据库持久化（新增 CommunicationParams 表）、默认配置、工厂注册；启动顺序
  调整为通讯管理器先于硬件管理器初始化，硬件工厂可按需引用已实例化的通讯连接。

[修复]
- Window_Closing 接线：e.Cancel 前置 + 重入门 + 释放顺序 Alarm→Production→Log
  （LogService 最后释放，确保关闭前的超时告警仍能落盘）。
- 启动进度提示节奏收敛到 Progress 回调集中延迟，移除各处散落的 Task.Delay。

v1.0.0 - 框架应用程序基础层初始版本：PrismApplication 抽象基类、MainWindow、MainWindowViewModelBase 及公共服务（IdleMonitor / PrismAlarmEventPublisher / TowerLightManager）。
		</PackageReleaseNotes>
		<Description>PF.AutoFramework 应用程序基础层：PrismApplication 抽象基类、主窗口、ViewModel 基类及公共服务；消费项目继承 PFApplicationBase 并实现抽象钩子即可完成接入。</Description>
		<GeneratePackageOnBuild Condition="'$(Configuration)' == 'Release'">true</GeneratePackageOnBuild>
	</PropertyGroup>

	<ItemGroup>
		<PackageReference Include="Prism.DryIoc" />
		<PackageReference Include="Microsoft.Extensions.Hosting" />
	</ItemGroup>
	<ItemGroup>
		<ProjectReference Include="..\PF.Infrastructure\PF.Infrastructure.csproj" />
		<ProjectReference Include="..\PF.Services\PF.Services.csproj" />
		<ProjectReference Include="..\PF.Data\PF.Data.csproj" />
		<ProjectReference Include="..\PF.SecsGem.DataBase\PF.SecsGem.DataBase.csproj" />
		<ProjectReference Include="..\PF.UI.Infrastructure\PF.UI.Infrastructure.csproj" />
		<ProjectReference Include="..\PF.CommonTools\PF.CommonTools.csproj" />
	</ItemGroup>
</Project>
