<Project Sdk="Microsoft.NET.Sdk">
	<Import Project="..\Common.Desktop.props" Condition="Exists('..\Common.Desktop.props')" />
	<ItemGroup>
		<PackageReference Include="Prism.Wpf" />
	</ItemGroup>
	<ItemGroup>
	  <ProjectReference Include="..\PF.CommonTools\PF.CommonTools.csproj" />
	  <ProjectReference Include="..\PF.Core\PF.Core.csproj" />
	  <ProjectReference Include="..\PF.Data\PF.Data.csproj" />
	  <ProjectReference Include="..\PF.UI.Infrastructure\PF.UI.Infrastructure.csproj" />
	</ItemGroup>

	<PropertyGroup>
		<IsModuleProject>true</IsModuleProject>
		<IsPackable>true</IsPackable>
		<Version>1.0.7</Version>
		<PackageReleaseNotes>
v1.0.7 (2026-08-28) — 界面操作日志接入：键目录 + 全量控件标记

[新增]
- OperationLogKeysParameter 键目录（[OperationLogKeyCatalog]，内层嵌套类名 = 页面名，const 字段 = Key，
  DescriptionAttribute = 默认描述），模块 OnInitialized 里 OperationLogKeyRegistry.
  RegisterAssembly(Assembly.GetExecutingAssembly()) 一行注册。
- 本模块页面根挂 OperationLog.PageName、可交互控件挂 OperationLog.Key：2 个页面
  11 个键，其中 6 个关键操作（恢复默认、修改参数数据类型、新增/删除/保存参数、对话框确认）默认启用记录；其余默认关闭，
  按项目在配置目录的 OperationLogConfig.xlsx 里勾选 Enabled 列即可打开，改过的值永远优先。
- 未标记/不支持的控件（密码框、Slider、DatePicker、页内导航选择器等）不产生任何日志。


v1.0.6 (2026-08-24) — 光源控制器参数视图支持通道数配置

[新增]
- CTSLightControllerParamView / HikComLightControllerParamView 新增"通道数"字段
  （Category=连接参数），对应 ConnectionParameters["ChannelCount"]；两个 Mapper 同步读写，
  缺省 4，与 PF.Infrastructure 1.0.12 的工厂缺省值一致。
- 配合 PF.Core 1.0.14 新增的 ILightController.ChannelCount：此前通道数只能手改配置 JSON，
  现可在参数界面的硬件配置对话框直接填写。

v1.0.5 (2026-08-17) — 新增海康串口光源控制器的硬件参数视图

[新增]
- HikComLightControllerParamView + Mapper，注册键 "HikComLightController"：
  设备基本信息 + 连接参数（串口通讯实例ID）。
  串口参数（波特率/校验位/数据位/停止位）不在硬件参数里配——该设备复用通讯层的串口实例，
  这里只填指向那条通讯配置的 InstanceId，串口本身在「通讯配置」里改。
  没有这一条时，该实现类的硬件配置在参数界面上打不开（ViewFactory 按
  ImplementationClassName 路由，未注册直接返回 null）。

[破坏性变更 — 康视达光源注册键拼写订正]
- 旧注册键 "CTS_LightControoller"（多一个 o）订正为 "CTS_LightController"，不保留兼容键。
- 起因是一个必现故障：CTSLightControllerParamViewMapper.ExtractSpecificData 写回的
  ImplementationClassName 一直是拼写正确的 "CTS_LightController"，而注册键与现场配置是
  拼错的那个，两者对不上——在参数界面编辑并保存一次康视达光源，保存后的配置就再也匹配不到
  硬件工厂，重启后该设备实例化不出来。修的方向是统一到正确拼写，而不是把映射器改成错的。
- 【升级须知】现场库里已有的硬件配置行仍带旧串，必须同步订正，否则该光源实例化不出来：
  · 消费项目的 App.xaml.cs：RegisterFactory 的键改成 "CTS_LightController"；
  · 现场数据库：把该设备配置的 ImplementationClassName 改成 "CTS_LightController"
    （或在硬件参数界面重建一次该设备）。
- Category 由 "LightController" 改为 "Light"，与默认配置一致，编辑保存不再翻转该字段。

v1.0.4 (2026-08-17) — 新增海康线阵相机 / 图像采集卡的硬件参数视图

[新增]
- HikFrameGrabberCardParamView + Mapper，注册键 "HikFrameGrabberCard"：
  设备基本信息 + 设备选定（序列号优先，留空按枚举索引选定）。采集卡是顶级设备，无父设备。
- HikLineScanCameraParamView + Mapper，注册键 "HikLineScanCamera"：
  设备基本信息（含"父设备ID(采集卡)"）+ 设备选定（序列号/枚举索引）+ 采集（图像缓存节点数）。
  · 父设备ID填采集卡的设备ID = 经采集卡取流（CameraLink/CXP/XoF），帧长与帧触发下发到采集卡
    节点树；留空 = GigE/USB 直连，下发到相机自身节点树。配置拓扑本身即表达链路类型，
    因此【没有】单独的"链路类型"字段。
  · 图像缓存节点数直接决定 SDK 侧内存占用（≈ 单帧字节数 × 本值），线扫大帧下必须按实际帧长
    下调，故做成配置项；小于 2 由设备层按 2 处理。
- 曝光、增益、行触发、编码器等【不在硬件参数里配】：它们随配方变化，由机构层按扫描任务下发，
  调试阶段用线阵相机调试面板逐项试（见 PF.Modules.Debug 1.0.7）。

v1.0.3 (2026-07-09) — 参数模块侧边栏图标调整

[变更]
- 系统配置参数 Icon：SettingIcon → Tune
- 硬件参数 Icon：DebugIcon → Chip
  配合 PF.Application.Base v1.0.3 新增的侧边栏悬停描边动画，原图标描边观感差或与其它
  调试页图标重复，一并替换。

v1.0.2 (2026-07-07) — Keyence SDK 扫码枪参数视图 + 计数竞态修复

[新增]
- KeyenceSdkBarcodeScanParamView + KeyenceSdkBarcodeScanParamViewMapper：
  基恩士 SDK 扫码枪的参数配置视图与映射器，接入 ParameterModule 硬件配置类型注册。
- MvCodeReaderBarcodeScanParamView + MvCodeReaderBarcodeScanParamViewMapper：
  补齐海康 MvCodeReaderSDK 扫码枪的参数配置视图（此前硬件工厂已注册但界面无法选中/配置）。

[删除]
- HKSdkBarcodeScanParamView + HKSdkBarcodeScanParamViewMapper：对应的 HKSdkBarcodeScan
  硬件实现从未注册工厂、全仓库零引用，随 PF.Infrastructure 一并删除，ParameterModule 中
  "HKSdkBarcodeScan" 注册项替换为 "MvCodeReaderBarcodeScan"。

[修复]
- ParameterViewModel.DeleteParameterAsync：Parameters.Remove 经 Dispatcher.InvokeAsync
  异步执行，但 UpdateCounts() 此前在其之前同步调用，导致删除后计数短暂偏高；现改为在
  InvokeAsync 回调内、Remove 之后再调用 UpdateCounts()。

v1.0.1 - 框架初始独立版本
		</PackageReleaseNotes>
		<Description>参数管理模块：硬件与系统参数的可视化编辑与验证</Description>
		<GeneratePackageOnBuild Condition="'$(Configuration)' == 'Release'">true</GeneratePackageOnBuild>
	</PropertyGroup>
</Project>