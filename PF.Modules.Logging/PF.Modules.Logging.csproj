<Project Sdk="Microsoft.NET.Sdk">
	<Import Project="..\Common.Desktop.props" Condition="Exists('..\Common.Desktop.props')" />
  <ItemGroup>
    <PackageReference Include="Prism.Wpf" />
	  <PackageReference Include="log4net" />
  </ItemGroup>
  <ItemGroup>
    <ProjectReference Include="..\PF.Data\PF.Data.csproj" />
    <ProjectReference Include="..\PF.Infrastructure\PF.Infrastructure.csproj" />
    <ProjectReference Include="..\PF.UI.Infrastructure\PF.UI.Infrastructure.csproj" />
  </ItemGroup>

	<PropertyGroup>
		<IsModuleProject>true</IsModuleProject>
		<IsPackable>true</IsPackable>
		<Version>1.0.4</Version>
		<PackageReleaseNotes>
v1.0.4 (2026-09-10) — 修复日志列表滚动卡顿（变行高场景）

[修复]
- LogListView（LogEntryTemplate 列表）与 LogManagementView（DataGrid）均把
  ScrollViewer.CanContentScroll 由 True 改为 False。两处日志条目本就允许
  TextWrapping="Wrap"/RowHeight="NaN" 折行变高，消费项目接入原始报文/JSON 展示后
  单条记录可能长达十几行，行高差异被放大；CanContentScroll=True 走逻辑项虚拟化，
  按"项数"而非像素计算滚动量，行高悬殊时表现为一卡一卡、鼠标滚轮体感极慢。改为
  按像素滚动（False）后滚动流畅度恢复正常，代价是失去逻辑项虚拟化的极限性能，
  但日志列表量级下无实际影响。

v1.0.3 (2026-08-28) — 界面操作日志接入：键目录 + 全量控件标记

[新增]
- OperationLogKeysLogging 键目录（[OperationLogKeyCatalog]，内层嵌套类名 = 页面名，const 字段 = Key，
  DescriptionAttribute = 默认描述），模块 OnInitialized 里 OperationLogKeyRegistry.
  RegisterAssembly(Assembly.GetExecutingAssembly()) 一行注册。
- 本模块页面根挂 OperationLog.PageName、可交互控件挂 OperationLog.Key：2 个页面
  13 个键，其中 2 个关键操作（清空日志、导出日志）默认启用记录；其余默认关闭，
  按项目在配置目录的 OperationLogConfig.xlsx 里勾选 Enabled 列即可打开，改过的值永远优先。
- 未标记/不支持的控件（密码框、Slider、DatePicker、页内导航选择器等）不产生任何日志。


v1.0.2 (2026-07-09) — 日志查询侧边栏图标调整

[变更]
- LogManagementView Icon：RunInfoIcon → History，避免与生产历史数据图标（此前同为
  RunInfoIcon）重复，并适配 PF.Application.Base v1.0.3 新增的侧边栏悬停描边动画。

v1.0.1 - 框架初始独立版本
		</PackageReleaseNotes>
		<Description>日志查看模块：实时日志展示、分类过滤</Description>
		<GeneratePackageOnBuild Condition="'$(Configuration)' == 'Release'">true</GeneratePackageOnBuild>
	</PropertyGroup>
</Project>